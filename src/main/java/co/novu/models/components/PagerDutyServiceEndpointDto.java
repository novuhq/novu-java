/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import co.novu.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonProperty;
import jakarta.annotation.Nonnull;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;


public class PagerDutyServiceEndpointDto {
    /**
     * PagerDuty Events API v2 integration key (32-character alphanumeric string). Encrypted at rest on the
     * channel endpoint (`endpoint` field).
     */
    @JsonProperty("routingKey")
    private String routingKey;

    /**
     * PagerDuty account region — determines the events API data-center endpoint.
     */
    @JsonProperty("region")
    private PagerDutyServiceEndpointDtoRegion region;

    @JsonCreator
    public PagerDutyServiceEndpointDto(
            @JsonProperty("routingKey") @Nonnull String routingKey,
            @JsonProperty("region") @Nonnull PagerDutyServiceEndpointDtoRegion region) {
        this.routingKey = Optional.ofNullable(routingKey)
            .orElseThrow(() -> new IllegalArgumentException("routingKey cannot be null"));
        this.region = Optional.ofNullable(region)
            .orElseThrow(() -> new IllegalArgumentException("region cannot be null"));
    }

    /**
     * PagerDuty Events API v2 integration key (32-character alphanumeric string). Encrypted at rest on the
     * channel endpoint (`endpoint` field).
     */
    public String routingKey() {
        return this.routingKey;
    }

    /**
     * PagerDuty account region — determines the events API data-center endpoint.
     */
    public PagerDutyServiceEndpointDtoRegion region() {
        return this.region;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * PagerDuty Events API v2 integration key (32-character alphanumeric string). Encrypted at rest on the
     * channel endpoint (`endpoint` field).
     */
    public PagerDutyServiceEndpointDto withRoutingKey(@Nonnull String routingKey) {
        this.routingKey = Utils.checkNotNull(routingKey, "routingKey");
        return this;
    }


    /**
     * PagerDuty account region — determines the events API data-center endpoint.
     */
    public PagerDutyServiceEndpointDto withRegion(@Nonnull PagerDutyServiceEndpointDtoRegion region) {
        this.region = Utils.checkNotNull(region, "region");
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        PagerDutyServiceEndpointDto other = (PagerDutyServiceEndpointDto) o;
        return 
            Utils.enhancedDeepEquals(this.routingKey, other.routingKey) &&
            Utils.enhancedDeepEquals(this.region, other.region);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            routingKey, region);
    }
    
    @Override
    public String toString() {
        return Utils.toString(PagerDutyServiceEndpointDto.class,
                "routingKey", routingKey,
                "region", region);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String routingKey;

        private PagerDutyServiceEndpointDtoRegion region;

        private Builder() {
          // force use of static builder() method
        }

        /**
         * PagerDuty Events API v2 integration key (32-character alphanumeric string). Encrypted at rest on the
         * channel endpoint (`endpoint` field).
         */
        public Builder routingKey(@Nonnull String routingKey) {
            this.routingKey = Utils.checkNotNull(routingKey, "routingKey");
            return this;
        }

        /**
         * PagerDuty account region — determines the events API data-center endpoint.
         */
        public Builder region(@Nonnull PagerDutyServiceEndpointDtoRegion region) {
            this.region = Utils.checkNotNull(region, "region");
            return this;
        }

        public PagerDutyServiceEndpointDto build() {
            return new PagerDutyServiceEndpointDto(
                routingKey, region);
        }

    }
}
