/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class DomainStatus {

    public static final DomainStatus PENDING = new DomainStatus("pending");
    public static final DomainStatus VERIFIED = new DomainStatus("verified");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, DomainStatus> values = createValuesMap();
    private static final Map<String, DomainStatusEnum> enums = createEnumsMap();

    private final String value;

    private DomainStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a DomainStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as DomainStatus
     */ 
    @JsonCreator
    public static DomainStatus of(String value) {
        synchronized (DomainStatus.class) {
            return values.computeIfAbsent(value, v -> new DomainStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<DomainStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        DomainStatus other = (DomainStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "DomainStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static DomainStatus[] values() {
        synchronized (DomainStatus.class) {
            return values.values().toArray(new DomainStatus[] {});
        }
    }

    private static final Map<String, DomainStatus> createValuesMap() {
        Map<String, DomainStatus> map = new LinkedHashMap<>();
        map.put("pending", PENDING);
        map.put("verified", VERIFIED);
        return map;
    }

    private static final Map<String, DomainStatusEnum> createEnumsMap() {
        Map<String, DomainStatusEnum> map = new HashMap<>();
        map.put("pending", DomainStatusEnum.PENDING);
        map.put("verified", DomainStatusEnum.VERIFIED);
        return map;
    }
    
    
    public enum DomainStatusEnum {

        PENDING("pending"),
        VERIFIED("verified"),;

        private final String value;

        private DomainStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

