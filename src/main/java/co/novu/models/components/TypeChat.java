/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class TypeChat {

    public static final TypeChat CHAT = new TypeChat("chat");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, TypeChat> values = createValuesMap();
    private static final Map<String, TypeChatEnum> enums = createEnumsMap();

    private final String value;

    private TypeChat(String value) {
        this.value = value;
    }

    /**
     * Returns a TypeChat with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as TypeChat
     */ 
    @JsonCreator
    public static TypeChat of(String value) {
        synchronized (TypeChat.class) {
            return values.computeIfAbsent(value, v -> new TypeChat(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<TypeChatEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        TypeChat other = (TypeChat) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "TypeChat [value=" + value + "]";
    }

    // return an array just like an enum
    public static TypeChat[] values() {
        synchronized (TypeChat.class) {
            return values.values().toArray(new TypeChat[] {});
        }
    }

    private static final Map<String, TypeChat> createValuesMap() {
        Map<String, TypeChat> map = new LinkedHashMap<>();
        map.put("chat", CHAT);
        return map;
    }

    private static final Map<String, TypeChatEnum> createEnumsMap() {
        Map<String, TypeChatEnum> map = new HashMap<>();
        map.put("chat", TypeChatEnum.CHAT);
        return map;
    }
    
    
    public enum TypeChatEnum {

        CHAT("chat"),;

        private final String value;

        private TypeChatEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

