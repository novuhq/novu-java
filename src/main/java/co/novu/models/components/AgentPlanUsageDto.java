/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import co.novu.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonProperty;
import jakarta.annotation.Nonnull;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;


public class AgentPlanUsageDto {
    /**
     * Current usage count for the resource in this environment.
     */
    @JsonProperty("used")
    private double used;

    /**
     * Amount included in the organization plan.
     */
    @JsonProperty("limit")
    private double limit;

    /**
     * Total agents in this environment, including inactive ones.
     */
    @JsonProperty("totalCreated")
    private double totalCreated;

    /**
     * Hard cap on total agents the organization can create per environment. For plan-limited tiers this is
     * the plan limit plus a small grace buffer; for unlimited tiers it is the platform system limit.
     */
    @JsonProperty("creationLimit")
    private double creationLimit;

    /**
     * Which constraint produced the limits. `plan` limits are lifted by upgrading; `system` limits
     * (platform cap or per-organization override) require contacting the Novu team.
     */
    @JsonProperty("limitSource")
    private LimitSource limitSource;

    @JsonCreator
    public AgentPlanUsageDto(
            @JsonProperty("used") double used,
            @JsonProperty("limit") double limit,
            @JsonProperty("totalCreated") double totalCreated,
            @JsonProperty("creationLimit") double creationLimit,
            @JsonProperty("limitSource") @Nonnull LimitSource limitSource) {
        this.used = used;
        this.limit = limit;
        this.totalCreated = totalCreated;
        this.creationLimit = creationLimit;
        this.limitSource = Optional.ofNullable(limitSource)
            .orElseThrow(() -> new IllegalArgumentException("limitSource cannot be null"));
    }

    /**
     * Current usage count for the resource in this environment.
     */
    public double used() {
        return this.used;
    }

    /**
     * Amount included in the organization plan.
     */
    public double limit() {
        return this.limit;
    }

    /**
     * Total agents in this environment, including inactive ones.
     */
    public double totalCreated() {
        return this.totalCreated;
    }

    /**
     * Hard cap on total agents the organization can create per environment. For plan-limited tiers this is
     * the plan limit plus a small grace buffer; for unlimited tiers it is the platform system limit.
     */
    public double creationLimit() {
        return this.creationLimit;
    }

    /**
     * Which constraint produced the limits. `plan` limits are lifted by upgrading; `system` limits
     * (platform cap or per-organization override) require contacting the Novu team.
     */
    public LimitSource limitSource() {
        return this.limitSource;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * Current usage count for the resource in this environment.
     */
    public AgentPlanUsageDto withUsed(double used) {
        this.used = used;
        return this;
    }


    /**
     * Amount included in the organization plan.
     */
    public AgentPlanUsageDto withLimit(double limit) {
        this.limit = limit;
        return this;
    }


    /**
     * Total agents in this environment, including inactive ones.
     */
    public AgentPlanUsageDto withTotalCreated(double totalCreated) {
        this.totalCreated = totalCreated;
        return this;
    }


    /**
     * Hard cap on total agents the organization can create per environment. For plan-limited tiers this is
     * the plan limit plus a small grace buffer; for unlimited tiers it is the platform system limit.
     */
    public AgentPlanUsageDto withCreationLimit(double creationLimit) {
        this.creationLimit = creationLimit;
        return this;
    }


    /**
     * Which constraint produced the limits. `plan` limits are lifted by upgrading; `system` limits
     * (platform cap or per-organization override) require contacting the Novu team.
     */
    public AgentPlanUsageDto withLimitSource(@Nonnull LimitSource limitSource) {
        this.limitSource = Utils.checkNotNull(limitSource, "limitSource");
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AgentPlanUsageDto other = (AgentPlanUsageDto) o;
        return 
            Utils.enhancedDeepEquals(this.used, other.used) &&
            Utils.enhancedDeepEquals(this.limit, other.limit) &&
            Utils.enhancedDeepEquals(this.totalCreated, other.totalCreated) &&
            Utils.enhancedDeepEquals(this.creationLimit, other.creationLimit) &&
            Utils.enhancedDeepEquals(this.limitSource, other.limitSource);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            used, limit, totalCreated,
            creationLimit, limitSource);
    }
    
    @Override
    public String toString() {
        return Utils.toString(AgentPlanUsageDto.class,
                "used", used,
                "limit", limit,
                "totalCreated", totalCreated,
                "creationLimit", creationLimit,
                "limitSource", limitSource);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private double used;

        private double limit;

        private double totalCreated;

        private double creationLimit;

        private LimitSource limitSource;

        private Builder() {
          // force use of static builder() method
        }

        /**
         * Current usage count for the resource in this environment.
         */
        public Builder used(double used) {
            this.used = used;
            return this;
        }

        /**
         * Amount included in the organization plan.
         */
        public Builder limit(double limit) {
            this.limit = limit;
            return this;
        }

        /**
         * Total agents in this environment, including inactive ones.
         */
        public Builder totalCreated(double totalCreated) {
            this.totalCreated = totalCreated;
            return this;
        }

        /**
         * Hard cap on total agents the organization can create per environment. For plan-limited tiers this is
         * the plan limit plus a small grace buffer; for unlimited tiers it is the platform system limit.
         */
        public Builder creationLimit(double creationLimit) {
            this.creationLimit = creationLimit;
            return this;
        }

        /**
         * Which constraint produced the limits. `plan` limits are lifted by upgrading; `system` limits
         * (platform cap or per-organization override) require contacting the Novu team.
         */
        public Builder limitSource(@Nonnull LimitSource limitSource) {
            this.limitSource = Utils.checkNotNull(limitSource, "limitSource");
            return this;
        }

        public AgentPlanUsageDto build() {
            return new AgentPlanUsageDto(
                used, limit, totalCreated,
                creationLimit, limitSource);
        }

    }
}
