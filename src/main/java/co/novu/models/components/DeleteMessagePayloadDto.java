/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import co.novu.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonProperty;
import jakarta.annotation.Nonnull;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;


public class DeleteMessagePayloadDto {
    /**
     * Platform message id to delete. Removes the rendered message only — history is preserved.
     */
    @JsonProperty("messageId")
    private String messageId;

    @JsonCreator
    public DeleteMessagePayloadDto(
            @JsonProperty("messageId") @Nonnull String messageId) {
        this.messageId = Optional.ofNullable(messageId)
            .orElseThrow(() -> new IllegalArgumentException("messageId cannot be null"));
    }

    /**
     * Platform message id to delete. Removes the rendered message only — history is preserved.
     */
    public String messageId() {
        return this.messageId;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * Platform message id to delete. Removes the rendered message only — history is preserved.
     */
    public DeleteMessagePayloadDto withMessageId(@Nonnull String messageId) {
        this.messageId = Utils.checkNotNull(messageId, "messageId");
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        DeleteMessagePayloadDto other = (DeleteMessagePayloadDto) o;
        return 
            Utils.enhancedDeepEquals(this.messageId, other.messageId);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            messageId);
    }
    
    @Override
    public String toString() {
        return Utils.toString(DeleteMessagePayloadDto.class,
                "messageId", messageId);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String messageId;

        private Builder() {
          // force use of static builder() method
        }

        /**
         * Platform message id to delete. Removes the rendered message only — history is preserved.
         */
        public Builder messageId(@Nonnull String messageId) {
            this.messageId = Utils.checkNotNull(messageId, "messageId");
            return this;
        }

        public DeleteMessagePayloadDto build() {
            return new DeleteMessagePayloadDto(
                messageId);
        }

    }
}
