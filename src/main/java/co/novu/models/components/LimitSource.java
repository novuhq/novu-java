/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * LimitSource
 * 
 * <p>Which constraint produced the limits. `plan` limits are lifted by upgrading; `system` limits
 * (platform cap or per-organization override) require contacting the Novu team.
 */
public class LimitSource {

    public static final LimitSource PLAN = new LimitSource("plan");
    public static final LimitSource SYSTEM = new LimitSource("system");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, LimitSource> values = createValuesMap();
    private static final Map<String, LimitSourceEnum> enums = createEnumsMap();

    private final String value;

    private LimitSource(String value) {
        this.value = value;
    }

    /**
     * Returns a LimitSource with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as LimitSource
     */ 
    @JsonCreator
    public static LimitSource of(String value) {
        synchronized (LimitSource.class) {
            return values.computeIfAbsent(value, v -> new LimitSource(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<LimitSourceEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        LimitSource other = (LimitSource) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "LimitSource [value=" + value + "]";
    }

    // return an array just like an enum
    public static LimitSource[] values() {
        synchronized (LimitSource.class) {
            return values.values().toArray(new LimitSource[] {});
        }
    }

    private static final Map<String, LimitSource> createValuesMap() {
        Map<String, LimitSource> map = new LinkedHashMap<>();
        map.put("plan", PLAN);
        map.put("system", SYSTEM);
        return map;
    }

    private static final Map<String, LimitSourceEnum> createEnumsMap() {
        Map<String, LimitSourceEnum> map = new HashMap<>();
        map.put("plan", LimitSourceEnum.PLAN);
        map.put("system", LimitSourceEnum.SYSTEM);
        return map;
    }
    
    
    public enum LimitSourceEnum {

        PLAN("plan"),
        SYSTEM("system"),;

        private final String value;

        private LimitSourceEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

