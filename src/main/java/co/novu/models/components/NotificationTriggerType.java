/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class NotificationTriggerType {

    public static final NotificationTriggerType EVENT = new NotificationTriggerType("event");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, NotificationTriggerType> values = createValuesMap();
    private static final Map<String, NotificationTriggerTypeEnum> enums = createEnumsMap();

    private final String value;

    private NotificationTriggerType(String value) {
        this.value = value;
    }

    /**
     * Returns a NotificationTriggerType with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as NotificationTriggerType
     */ 
    @JsonCreator
    public static NotificationTriggerType of(String value) {
        synchronized (NotificationTriggerType.class) {
            return values.computeIfAbsent(value, v -> new NotificationTriggerType(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<NotificationTriggerTypeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        NotificationTriggerType other = (NotificationTriggerType) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "NotificationTriggerType [value=" + value + "]";
    }

    // return an array just like an enum
    public static NotificationTriggerType[] values() {
        synchronized (NotificationTriggerType.class) {
            return values.values().toArray(new NotificationTriggerType[] {});
        }
    }

    private static final Map<String, NotificationTriggerType> createValuesMap() {
        Map<String, NotificationTriggerType> map = new LinkedHashMap<>();
        map.put("event", EVENT);
        return map;
    }

    private static final Map<String, NotificationTriggerTypeEnum> createEnumsMap() {
        Map<String, NotificationTriggerTypeEnum> map = new HashMap<>();
        map.put("event", NotificationTriggerTypeEnum.EVENT);
        return map;
    }
    
    
    public enum NotificationTriggerTypeEnum {

        EVENT("event"),;

        private final String value;

        private NotificationTriggerTypeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

