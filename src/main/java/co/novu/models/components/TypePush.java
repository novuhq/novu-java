/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class TypePush {

    public static final TypePush PUSH = new TypePush("push");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, TypePush> values = createValuesMap();
    private static final Map<String, TypePushEnum> enums = createEnumsMap();

    private final String value;

    private TypePush(String value) {
        this.value = value;
    }

    /**
     * Returns a TypePush with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as TypePush
     */ 
    @JsonCreator
    public static TypePush of(String value) {
        synchronized (TypePush.class) {
            return values.computeIfAbsent(value, v -> new TypePush(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<TypePushEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        TypePush other = (TypePush) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "TypePush [value=" + value + "]";
    }

    // return an array just like an enum
    public static TypePush[] values() {
        synchronized (TypePush.class) {
            return values.values().toArray(new TypePush[] {});
        }
    }

    private static final Map<String, TypePush> createValuesMap() {
        Map<String, TypePush> map = new LinkedHashMap<>();
        map.put("push", PUSH);
        return map;
    }

    private static final Map<String, TypePushEnum> createEnumsMap() {
        Map<String, TypePushEnum> map = new HashMap<>();
        map.put("push", TypePushEnum.PUSH);
        return map;
    }
    
    
    public enum TypePushEnum {

        PUSH("push"),;

        private final String value;

        private TypePushEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

