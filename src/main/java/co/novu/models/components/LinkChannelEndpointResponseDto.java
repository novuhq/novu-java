/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import co.novu.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.util.Map;
import java.util.Optional;


public class LinkChannelEndpointResponseDto {
    /**
     * URL the subscriber opens to link their chat identity (OAuth URL or deep link)
     */
    @JsonProperty("url")
    private String url;

    /**
     * Provider-specific metadata returned alongside the link URL
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("providerMetadata")
    private Map<String, Object> providerMetadata;

    @JsonCreator
    public LinkChannelEndpointResponseDto(
            @JsonProperty("url") @Nonnull String url,
            @JsonProperty("providerMetadata") @Nullable Map<String, Object> providerMetadata) {
        this.url = Optional.ofNullable(url)
            .orElseThrow(() -> new IllegalArgumentException("url cannot be null"));
        this.providerMetadata = providerMetadata;
    }
    
    public LinkChannelEndpointResponseDto(
            @Nonnull String url) {
        this(url, null);
    }

    /**
     * URL the subscriber opens to link their chat identity (OAuth URL or deep link)
     */
    public String url() {
        return this.url;
    }

    /**
     * Provider-specific metadata returned alongside the link URL
     */
    public Optional<Map<String, Object>> providerMetadata() {
        return Optional.ofNullable(this.providerMetadata);
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * URL the subscriber opens to link their chat identity (OAuth URL or deep link)
     */
    public LinkChannelEndpointResponseDto withUrl(@Nonnull String url) {
        this.url = Utils.checkNotNull(url, "url");
        return this;
    }


    /**
     * Provider-specific metadata returned alongside the link URL
     */
    public LinkChannelEndpointResponseDto withProviderMetadata(@Nullable Map<String, Object> providerMetadata) {
        this.providerMetadata = providerMetadata;
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        LinkChannelEndpointResponseDto other = (LinkChannelEndpointResponseDto) o;
        return 
            Utils.enhancedDeepEquals(this.url, other.url) &&
            Utils.enhancedDeepEquals(this.providerMetadata, other.providerMetadata);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            url, providerMetadata);
    }
    
    @Override
    public String toString() {
        return Utils.toString(LinkChannelEndpointResponseDto.class,
                "url", url,
                "providerMetadata", providerMetadata);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String url;

        private Map<String, Object> providerMetadata;

        private Builder() {
          // force use of static builder() method
        }

        /**
         * URL the subscriber opens to link their chat identity (OAuth URL or deep link)
         */
        public Builder url(@Nonnull String url) {
            this.url = Utils.checkNotNull(url, "url");
            return this;
        }

        /**
         * Provider-specific metadata returned alongside the link URL
         */
        public Builder providerMetadata(@Nullable Map<String, Object> providerMetadata) {
            this.providerMetadata = providerMetadata;
            return this;
        }

        public LinkChannelEndpointResponseDto build() {
            return new LinkChannelEndpointResponseDto(
                url, providerMetadata);
        }

    }
}
