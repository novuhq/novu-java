/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class On {

    public static final On SUBSCRIBER = new On("subscriber");
    public static final On PAYLOAD = new On("payload");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, On> values = createValuesMap();
    private static final Map<String, OnEnum> enums = createEnumsMap();

    private final String value;

    private On(String value) {
        this.value = value;
    }

    /**
     * Returns a On with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as On
     */ 
    @JsonCreator
    public static On of(String value) {
        synchronized (On.class) {
            return values.computeIfAbsent(value, v -> new On(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<OnEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        On other = (On) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "On [value=" + value + "]";
    }

    // return an array just like an enum
    public static On[] values() {
        synchronized (On.class) {
            return values.values().toArray(new On[] {});
        }
    }

    private static final Map<String, On> createValuesMap() {
        Map<String, On> map = new LinkedHashMap<>();
        map.put("subscriber", SUBSCRIBER);
        map.put("payload", PAYLOAD);
        return map;
    }

    private static final Map<String, OnEnum> createEnumsMap() {
        Map<String, OnEnum> map = new HashMap<>();
        map.put("subscriber", OnEnum.SUBSCRIBER);
        map.put("payload", OnEnum.PAYLOAD);
        return map;
    }
    
    
    public enum OnEnum {

        SUBSCRIBER("subscriber"),
        PAYLOAD("payload"),;

        private final String value;

        private OnEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

