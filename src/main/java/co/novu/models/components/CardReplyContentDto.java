/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import co.novu.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.util.List;
import java.util.Map;
import java.util.Optional;


public class CardReplyContentDto {
    /**
     * Interactive card as a Chat SDK element tree (`type: "card"`). Prefer building with `@novu/framework`
     * Card helpers when using TypeScript.
     */
    @JsonProperty("card")
    private Map<String, Object> card;

    /**
     * Optional attachments (max 15). Supported alongside cards on platforms that allow it (e.g. WhatsApp).
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("files")
    private List<FileRefDto> files;

    @JsonCreator
    public CardReplyContentDto(
            @JsonProperty("card") @Nonnull Map<String, Object> card,
            @JsonProperty("files") @Nullable List<FileRefDto> files) {
        card = Utils.emptyMapIfNull(card);
        this.card = Optional.ofNullable(card)
            .orElseThrow(() -> new IllegalArgumentException("card cannot be null"));
        this.files = files;
    }
    
    public CardReplyContentDto(
            @Nonnull Map<String, Object> card) {
        this(card, null);
    }

    /**
     * Interactive card as a Chat SDK element tree (`type: "card"`). Prefer building with `@novu/framework`
     * Card helpers when using TypeScript.
     */
    public Map<String, Object> card() {
        return this.card;
    }

    /**
     * Optional attachments (max 15). Supported alongside cards on platforms that allow it (e.g. WhatsApp).
     */
    public Optional<List<FileRefDto>> files() {
        return Optional.ofNullable(this.files);
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * Interactive card as a Chat SDK element tree (`type: "card"`). Prefer building with `@novu/framework`
     * Card helpers when using TypeScript.
     */
    public CardReplyContentDto withCard(@Nonnull Map<String, Object> card) {
        this.card = Utils.checkNotNull(card, "card");
        return this;
    }


    /**
     * Optional attachments (max 15). Supported alongside cards on platforms that allow it (e.g. WhatsApp).
     */
    public CardReplyContentDto withFiles(@Nullable List<FileRefDto> files) {
        this.files = files;
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        CardReplyContentDto other = (CardReplyContentDto) o;
        return 
            Utils.enhancedDeepEquals(this.card, other.card) &&
            Utils.enhancedDeepEquals(this.files, other.files);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            card, files);
    }
    
    @Override
    public String toString() {
        return Utils.toString(CardReplyContentDto.class,
                "card", card,
                "files", files);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private Map<String, Object> card;

        private List<FileRefDto> files;

        private Builder() {
          // force use of static builder() method
        }

        /**
         * Interactive card as a Chat SDK element tree (`type: "card"`). Prefer building with `@novu/framework`
         * Card helpers when using TypeScript.
         */
        public Builder card(@Nonnull Map<String, Object> card) {
            this.card = Utils.checkNotNull(card, "card");
            return this;
        }

        /**
         * Optional attachments (max 15). Supported alongside cards on platforms that allow it (e.g. WhatsApp).
         */
        public Builder files(@Nullable List<FileRefDto> files) {
            this.files = files;
            return this;
        }

        public CardReplyContentDto build() {
            return new CardReplyContentDto(
                card, files);
        }

    }
}
