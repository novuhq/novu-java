/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class TypeDigest {

    public static final TypeDigest DIGEST = new TypeDigest("digest");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, TypeDigest> values = createValuesMap();
    private static final Map<String, TypeDigestEnum> enums = createEnumsMap();

    private final String value;

    private TypeDigest(String value) {
        this.value = value;
    }

    /**
     * Returns a TypeDigest with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as TypeDigest
     */ 
    @JsonCreator
    public static TypeDigest of(String value) {
        synchronized (TypeDigest.class) {
            return values.computeIfAbsent(value, v -> new TypeDigest(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<TypeDigestEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        TypeDigest other = (TypeDigest) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "TypeDigest [value=" + value + "]";
    }

    // return an array just like an enum
    public static TypeDigest[] values() {
        synchronized (TypeDigest.class) {
            return values.values().toArray(new TypeDigest[] {});
        }
    }

    private static final Map<String, TypeDigest> createValuesMap() {
        Map<String, TypeDigest> map = new LinkedHashMap<>();
        map.put("digest", DIGEST);
        return map;
    }

    private static final Map<String, TypeDigestEnum> createEnumsMap() {
        Map<String, TypeDigestEnum> map = new HashMap<>();
        map.put("digest", TypeDigestEnum.DIGEST);
        return map;
    }
    
    
    public enum TypeDigestEnum {

        DIGEST("digest"),;

        private final String value;

        private TypeDigestEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

