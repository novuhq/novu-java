/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class TypeDelay {

    public static final TypeDelay DELAY = new TypeDelay("delay");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, TypeDelay> values = createValuesMap();
    private static final Map<String, TypeDelayEnum> enums = createEnumsMap();

    private final String value;

    private TypeDelay(String value) {
        this.value = value;
    }

    /**
     * Returns a TypeDelay with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as TypeDelay
     */ 
    @JsonCreator
    public static TypeDelay of(String value) {
        synchronized (TypeDelay.class) {
            return values.computeIfAbsent(value, v -> new TypeDelay(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<TypeDelayEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        TypeDelay other = (TypeDelay) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "TypeDelay [value=" + value + "]";
    }

    // return an array just like an enum
    public static TypeDelay[] values() {
        synchronized (TypeDelay.class) {
            return values.values().toArray(new TypeDelay[] {});
        }
    }

    private static final Map<String, TypeDelay> createValuesMap() {
        Map<String, TypeDelay> map = new LinkedHashMap<>();
        map.put("delay", DELAY);
        return map;
    }

    private static final Map<String, TypeDelayEnum> createEnumsMap() {
        Map<String, TypeDelayEnum> map = new HashMap<>();
        map.put("delay", TypeDelayEnum.DELAY);
        return map;
    }
    
    
    public enum TypeDelayEnum {

        DELAY("delay"),;

        private final String value;

        private TypeDelayEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

