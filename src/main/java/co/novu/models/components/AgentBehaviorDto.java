/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import co.novu.utils.LazySingletonValue;
import co.novu.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.core.type.TypeReference;
import jakarta.annotation.Nullable;
import java.lang.Boolean;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;
import org.openapitools.jackson.nullable.JsonNullable;


public class AgentBehaviorDto {
    /**
     * Acknowledge incoming messages. On platforms that support a native typing indicator (e.g. Slack,
     * WhatsApp, Microsoft Teams, Telegram), shows a "Typing…" indicator while the agent processes the
     * message.
     * 
     * <p>On platforms that do not (e.g. Email), reacts with an "eyes" emoji to the first inbound message in a
     * thread. Default: true
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("acknowledgeOnReceived")
    private Boolean acknowledgeOnReceived;

    /**
     * Cross-platform emoji name for resolved conversations (e.g. "check", "star"). Set to null to disable.
     * 
     * <p>Default: "check"
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("reactionOnResolved")
    private JsonNullable<ReactionOnResolved> reactionOnResolved;

    /**
     * Controls whether the agent accepts inbound messages from senders not yet linked to a subscriber,
     * across all channels. "open" on managed agents auto-creates a lightweight subscriber so the agent can
     * reply; on custom-code / self-hosted agents, the turn is forwarded to the bridge with a null
     * subscriber. "restricted" rejects unknown senders with a managed denial reply (any runtime).
     * 
     * <p>Optional on update (partial PATCH). Persisted agents always have a value — managed create defaults
     * to "open"; self-hosted create defaults to "restricted".
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("subscriberAccess")
    private SubscriberAccess subscriberAccess;

    @JsonCreator
    public AgentBehaviorDto(
            @JsonProperty("acknowledgeOnReceived") @Nullable Boolean acknowledgeOnReceived,
            @JsonProperty("reactionOnResolved") @Nullable JsonNullable<ReactionOnResolved> reactionOnResolved,
            @JsonProperty("subscriberAccess") @Nullable SubscriberAccess subscriberAccess) {
        this.acknowledgeOnReceived = Optional.ofNullable(acknowledgeOnReceived)
            .orElse(Builder._SINGLETON_VALUE_AcknowledgeOnReceived.value());
        this.reactionOnResolved = Optional.ofNullable(reactionOnResolved)
            .orElse(JsonNullable.undefined());
        this.subscriberAccess = subscriberAccess;
    }
    
    public AgentBehaviorDto() {
        this(null, null, null);
    }

    /**
     * Acknowledge incoming messages. On platforms that support a native typing indicator (e.g. Slack,
     * WhatsApp, Microsoft Teams, Telegram), shows a "Typing…" indicator while the agent processes the
     * message.
     * 
     * <p>On platforms that do not (e.g. Email), reacts with an "eyes" emoji to the first inbound message in a
     * thread. Default: true
     */
    public Optional<Boolean> acknowledgeOnReceived() {
        return Optional.ofNullable(this.acknowledgeOnReceived);
    }

    /**
     * Cross-platform emoji name for resolved conversations (e.g. "check", "star"). Set to null to disable.
     * 
     * <p>Default: "check"
     */
    public JsonNullable<ReactionOnResolved> reactionOnResolved() {
        return this.reactionOnResolved;
    }

    /**
     * Controls whether the agent accepts inbound messages from senders not yet linked to a subscriber,
     * across all channels. "open" on managed agents auto-creates a lightweight subscriber so the agent can
     * reply; on custom-code / self-hosted agents, the turn is forwarded to the bridge with a null
     * subscriber. "restricted" rejects unknown senders with a managed denial reply (any runtime).
     * 
     * <p>Optional on update (partial PATCH). Persisted agents always have a value — managed create defaults
     * to "open"; self-hosted create defaults to "restricted".
     */
    public Optional<SubscriberAccess> subscriberAccess() {
        return Optional.ofNullable(this.subscriberAccess);
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * Acknowledge incoming messages. On platforms that support a native typing indicator (e.g. Slack,
     * WhatsApp, Microsoft Teams, Telegram), shows a "Typing…" indicator while the agent processes the
     * message.
     * 
     * <p>On platforms that do not (e.g. Email), reacts with an "eyes" emoji to the first inbound message in a
     * thread. Default: true
     */
    public AgentBehaviorDto withAcknowledgeOnReceived(@Nullable Boolean acknowledgeOnReceived) {
        this.acknowledgeOnReceived = acknowledgeOnReceived;
        return this;
    }


    /**
     * Cross-platform emoji name for resolved conversations (e.g. "check", "star"). Set to null to disable.
     * 
     * <p>Default: "check"
     */
    public AgentBehaviorDto withReactionOnResolved(@Nullable ReactionOnResolved reactionOnResolved) {
        this.reactionOnResolved = JsonNullable.of(reactionOnResolved);
        return this;
    }


    /**
     * Controls whether the agent accepts inbound messages from senders not yet linked to a subscriber,
     * across all channels. "open" on managed agents auto-creates a lightweight subscriber so the agent can
     * reply; on custom-code / self-hosted agents, the turn is forwarded to the bridge with a null
     * subscriber. "restricted" rejects unknown senders with a managed denial reply (any runtime).
     * 
     * <p>Optional on update (partial PATCH). Persisted agents always have a value — managed create defaults
     * to "open"; self-hosted create defaults to "restricted".
     */
    public AgentBehaviorDto withSubscriberAccess(@Nullable SubscriberAccess subscriberAccess) {
        this.subscriberAccess = subscriberAccess;
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AgentBehaviorDto other = (AgentBehaviorDto) o;
        return 
            Utils.enhancedDeepEquals(this.acknowledgeOnReceived, other.acknowledgeOnReceived) &&
            Utils.enhancedDeepEquals(this.reactionOnResolved, other.reactionOnResolved) &&
            Utils.enhancedDeepEquals(this.subscriberAccess, other.subscriberAccess);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            acknowledgeOnReceived, reactionOnResolved, subscriberAccess);
    }
    
    @Override
    public String toString() {
        return Utils.toString(AgentBehaviorDto.class,
                "acknowledgeOnReceived", acknowledgeOnReceived,
                "reactionOnResolved", reactionOnResolved,
                "subscriberAccess", subscriberAccess);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private Boolean acknowledgeOnReceived;

        private JsonNullable<ReactionOnResolved> reactionOnResolved;

        private SubscriberAccess subscriberAccess;

        private Builder() {
          // force use of static builder() method
        }

        /**
         * Acknowledge incoming messages. On platforms that support a native typing indicator (e.g. Slack,
         * WhatsApp, Microsoft Teams, Telegram), shows a "Typing…" indicator while the agent processes the
         * message.
         * 
         * <p>On platforms that do not (e.g. Email), reacts with an "eyes" emoji to the first inbound message in a
         * thread. Default: true
         */
        public Builder acknowledgeOnReceived(@Nullable Boolean acknowledgeOnReceived) {
            this.acknowledgeOnReceived = acknowledgeOnReceived;
            return this;
        }

        /**
         * Cross-platform emoji name for resolved conversations (e.g. "check", "star"). Set to null to disable.
         * 
         * <p>Default: "check"
         */
        public Builder reactionOnResolved(@Nullable ReactionOnResolved reactionOnResolved) {
            this.reactionOnResolved = JsonNullable.of(reactionOnResolved);
            return this;
        }

        /**
         * Controls whether the agent accepts inbound messages from senders not yet linked to a subscriber,
         * across all channels. "open" on managed agents auto-creates a lightweight subscriber so the agent can
         * reply; on custom-code / self-hosted agents, the turn is forwarded to the bridge with a null
         * subscriber. "restricted" rejects unknown senders with a managed denial reply (any runtime).
         * 
         * <p>Optional on update (partial PATCH). Persisted agents always have a value — managed create defaults
         * to "open"; self-hosted create defaults to "restricted".
         */
        public Builder subscriberAccess(@Nullable SubscriberAccess subscriberAccess) {
            this.subscriberAccess = subscriberAccess;
            return this;
        }

        public AgentBehaviorDto build() {
            return new AgentBehaviorDto(
                acknowledgeOnReceived, reactionOnResolved, subscriberAccess);
        }


        private static final LazySingletonValue<Boolean> _SINGLETON_VALUE_AcknowledgeOnReceived =
                new LazySingletonValue<>(
                        "acknowledgeOnReceived",
                        "true",
                        new TypeReference<Boolean>() {});
    }
}
