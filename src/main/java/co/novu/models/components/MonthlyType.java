/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class MonthlyType {

    public static final MonthlyType EACH = new MonthlyType("each");
    public static final MonthlyType ON = new MonthlyType("on");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, MonthlyType> values = createValuesMap();
    private static final Map<String, MonthlyTypeEnum> enums = createEnumsMap();

    private final String value;

    private MonthlyType(String value) {
        this.value = value;
    }

    /**
     * Returns a MonthlyType with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as MonthlyType
     */ 
    @JsonCreator
    public static MonthlyType of(String value) {
        synchronized (MonthlyType.class) {
            return values.computeIfAbsent(value, v -> new MonthlyType(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<MonthlyTypeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        MonthlyType other = (MonthlyType) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "MonthlyType [value=" + value + "]";
    }

    // return an array just like an enum
    public static MonthlyType[] values() {
        synchronized (MonthlyType.class) {
            return values.values().toArray(new MonthlyType[] {});
        }
    }

    private static final Map<String, MonthlyType> createValuesMap() {
        Map<String, MonthlyType> map = new LinkedHashMap<>();
        map.put("each", EACH);
        map.put("on", ON);
        return map;
    }

    private static final Map<String, MonthlyTypeEnum> createEnumsMap() {
        Map<String, MonthlyTypeEnum> map = new HashMap<>();
        map.put("each", MonthlyTypeEnum.EACH);
        map.put("on", MonthlyTypeEnum.ON);
        return map;
    }
    
    
    public enum MonthlyTypeEnum {

        EACH("each"),
        ON("on"),;

        private final String value;

        private MonthlyTypeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

