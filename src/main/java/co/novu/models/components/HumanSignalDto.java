/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import co.novu.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.Double;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;


public class HumanSignalDto {

    @JsonProperty("type")
    private HumanSignalDtoType type;

    /**
     * Interaction verb queued by `ctx.ask` / `ctx.approve` / `ctx.choose` / `ctx.tell`.
     */
    @JsonProperty("kind")
    private HumanSignalDtoKind kind;

    /**
     * Markdown, HITL chrome, or a posted Card element (`type: "card"`) from `{ render }`.
     */
    @JsonProperty("card")
    private Object card;

    /**
     * Client-minted id returned by the framework helper; echoed on `ctx.humanResponse.requestId`.
     */
    @JsonProperty("requestId")
    private String requestId;

    /**
     * When set, pending chrome buttons use `human:{actionIdentifier}:…` (the client `requestId` for
     * `renderApprove`).
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("actionIdentifier")
    private String actionIdentifier;

    /**
     * Attribution label rendered in the card.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("from")
    private String from;

    /**
     * Seconds until the interaction expires. Default 86400 (24h), max 432000.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("ttlSeconds")
    private Double ttlSeconds;

    /**
     * Novu subscriberId(s) allowed to settle this interaction (max 50). First valid answer wins. When
     * omitted, the conversation subscriber is used.
     * 
     * <p>Subscriber ids only — not workflow topic recipients.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("to")
    private HumanSignalDtoTo to;

    @JsonCreator
    public HumanSignalDto(
            @JsonProperty("type") @Nonnull HumanSignalDtoType type,
            @JsonProperty("kind") @Nonnull HumanSignalDtoKind kind,
            @JsonProperty("card") @Nonnull Object card,
            @JsonProperty("requestId") @Nonnull String requestId,
            @JsonProperty("actionIdentifier") @Nullable String actionIdentifier,
            @JsonProperty("from") @Nullable String from,
            @JsonProperty("ttlSeconds") @Nullable Double ttlSeconds,
            @JsonProperty("to") @Nullable HumanSignalDtoTo to) {
        this.type = Optional.ofNullable(type)
            .orElseThrow(() -> new IllegalArgumentException("type cannot be null"));
        this.kind = Optional.ofNullable(kind)
            .orElseThrow(() -> new IllegalArgumentException("kind cannot be null"));
        this.card = Optional.ofNullable(card)
            .orElseThrow(() -> new IllegalArgumentException("card cannot be null"));
        this.requestId = Optional.ofNullable(requestId)
            .orElseThrow(() -> new IllegalArgumentException("requestId cannot be null"));
        this.actionIdentifier = actionIdentifier;
        this.from = from;
        this.ttlSeconds = ttlSeconds;
        this.to = to;
    }
    
    public HumanSignalDto(
            @Nonnull HumanSignalDtoType type,
            @Nonnull HumanSignalDtoKind kind,
            @Nonnull Object card,
            @Nonnull String requestId) {
        this(type, kind, card,
            requestId, null, null,
            null, null);
    }

    public HumanSignalDtoType type() {
        return this.type;
    }

    /**
     * Interaction verb queued by `ctx.ask` / `ctx.approve` / `ctx.choose` / `ctx.tell`.
     */
    public HumanSignalDtoKind kind() {
        return this.kind;
    }

    /**
     * Markdown, HITL chrome, or a posted Card element (`type: "card"`) from `{ render }`.
     */
    public Object card() {
        return this.card;
    }

    /**
     * Client-minted id returned by the framework helper; echoed on `ctx.humanResponse.requestId`.
     */
    public String requestId() {
        return this.requestId;
    }

    /**
     * When set, pending chrome buttons use `human:{actionIdentifier}:…` (the client `requestId` for
     * `renderApprove`).
     */
    public Optional<String> actionIdentifier() {
        return Optional.ofNullable(this.actionIdentifier);
    }

    /**
     * Attribution label rendered in the card.
     */
    public Optional<String> from() {
        return Optional.ofNullable(this.from);
    }

    /**
     * Seconds until the interaction expires. Default 86400 (24h), max 432000.
     */
    public Optional<Double> ttlSeconds() {
        return Optional.ofNullable(this.ttlSeconds);
    }

    /**
     * Novu subscriberId(s) allowed to settle this interaction (max 50). First valid answer wins. When
     * omitted, the conversation subscriber is used.
     * 
     * <p>Subscriber ids only — not workflow topic recipients.
     */
    public Optional<HumanSignalDtoTo> to() {
        return Optional.ofNullable(this.to);
    }

    public static Builder builder() {
        return new Builder();
    }


    public HumanSignalDto withType(@Nonnull HumanSignalDtoType type) {
        this.type = Utils.checkNotNull(type, "type");
        return this;
    }


    /**
     * Interaction verb queued by `ctx.ask` / `ctx.approve` / `ctx.choose` / `ctx.tell`.
     */
    public HumanSignalDto withKind(@Nonnull HumanSignalDtoKind kind) {
        this.kind = Utils.checkNotNull(kind, "kind");
        return this;
    }


    /**
     * Markdown, HITL chrome, or a posted Card element (`type: "card"`) from `{ render }`.
     */
    public HumanSignalDto withCard(@Nonnull Object card) {
        this.card = Utils.checkNotNull(card, "card");
        return this;
    }


    /**
     * Client-minted id returned by the framework helper; echoed on `ctx.humanResponse.requestId`.
     */
    public HumanSignalDto withRequestId(@Nonnull String requestId) {
        this.requestId = Utils.checkNotNull(requestId, "requestId");
        return this;
    }


    /**
     * When set, pending chrome buttons use `human:{actionIdentifier}:…` (the client `requestId` for
     * `renderApprove`).
     */
    public HumanSignalDto withActionIdentifier(@Nullable String actionIdentifier) {
        this.actionIdentifier = actionIdentifier;
        return this;
    }


    /**
     * Attribution label rendered in the card.
     */
    public HumanSignalDto withFrom(@Nullable String from) {
        this.from = from;
        return this;
    }


    /**
     * Seconds until the interaction expires. Default 86400 (24h), max 432000.
     */
    public HumanSignalDto withTtlSeconds(@Nullable Double ttlSeconds) {
        this.ttlSeconds = ttlSeconds;
        return this;
    }


    /**
     * Novu subscriberId(s) allowed to settle this interaction (max 50). First valid answer wins. When
     * omitted, the conversation subscriber is used.
     * 
     * <p>Subscriber ids only — not workflow topic recipients.
     */
    public HumanSignalDto withTo(@Nullable HumanSignalDtoTo to) {
        this.to = to;
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        HumanSignalDto other = (HumanSignalDto) o;
        return 
            Utils.enhancedDeepEquals(this.type, other.type) &&
            Utils.enhancedDeepEquals(this.kind, other.kind) &&
            Utils.enhancedDeepEquals(this.card, other.card) &&
            Utils.enhancedDeepEquals(this.requestId, other.requestId) &&
            Utils.enhancedDeepEquals(this.actionIdentifier, other.actionIdentifier) &&
            Utils.enhancedDeepEquals(this.from, other.from) &&
            Utils.enhancedDeepEquals(this.ttlSeconds, other.ttlSeconds) &&
            Utils.enhancedDeepEquals(this.to, other.to);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            type, kind, card,
            requestId, actionIdentifier, from,
            ttlSeconds, to);
    }
    
    @Override
    public String toString() {
        return Utils.toString(HumanSignalDto.class,
                "type", type,
                "kind", kind,
                "card", card,
                "requestId", requestId,
                "actionIdentifier", actionIdentifier,
                "from", from,
                "ttlSeconds", ttlSeconds,
                "to", to);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private HumanSignalDtoType type;

        private HumanSignalDtoKind kind;

        private Object card;

        private String requestId;

        private String actionIdentifier;

        private String from;

        private Double ttlSeconds;

        private HumanSignalDtoTo to;

        private Builder() {
          // force use of static builder() method
        }

        public Builder type(@Nonnull HumanSignalDtoType type) {
            this.type = Utils.checkNotNull(type, "type");
            return this;
        }

        /**
         * Interaction verb queued by `ctx.ask` / `ctx.approve` / `ctx.choose` / `ctx.tell`.
         */
        public Builder kind(@Nonnull HumanSignalDtoKind kind) {
            this.kind = Utils.checkNotNull(kind, "kind");
            return this;
        }

        /**
         * Markdown, HITL chrome, or a posted Card element (`type: "card"`) from `{ render }`.
         */
        public Builder card(@Nonnull Object card) {
            this.card = Utils.checkNotNull(card, "card");
            return this;
        }

        /**
         * Client-minted id returned by the framework helper; echoed on `ctx.humanResponse.requestId`.
         */
        public Builder requestId(@Nonnull String requestId) {
            this.requestId = Utils.checkNotNull(requestId, "requestId");
            return this;
        }

        /**
         * When set, pending chrome buttons use `human:{actionIdentifier}:…` (the client `requestId` for
         * `renderApprove`).
         */
        public Builder actionIdentifier(@Nullable String actionIdentifier) {
            this.actionIdentifier = actionIdentifier;
            return this;
        }

        /**
         * Attribution label rendered in the card.
         */
        public Builder from(@Nullable String from) {
            this.from = from;
            return this;
        }

        /**
         * Seconds until the interaction expires. Default 86400 (24h), max 432000.
         */
        public Builder ttlSeconds(@Nullable Double ttlSeconds) {
            this.ttlSeconds = ttlSeconds;
            return this;
        }

        /**
         * Novu subscriberId(s) allowed to settle this interaction (max 50). First valid answer wins. When
         * omitted, the conversation subscriber is used.
         * 
         * <p>Subscriber ids only — not workflow topic recipients.
         */
        public Builder to(@Nullable HumanSignalDtoTo to) {
            this.to = to;
            return this;
        }

        public HumanSignalDto build() {
            return new HumanSignalDto(
                type, kind, card,
                requestId, actionIdentifier, from,
                ttlSeconds, to);
        }

    }
}
