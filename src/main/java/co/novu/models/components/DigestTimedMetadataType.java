/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class DigestTimedMetadataType {

    public static final DigestTimedMetadataType TIMED = new DigestTimedMetadataType("timed");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, DigestTimedMetadataType> values = createValuesMap();
    private static final Map<String, DigestTimedMetadataTypeEnum> enums = createEnumsMap();

    private final String value;

    private DigestTimedMetadataType(String value) {
        this.value = value;
    }

    /**
     * Returns a DigestTimedMetadataType with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as DigestTimedMetadataType
     */ 
    @JsonCreator
    public static DigestTimedMetadataType of(String value) {
        synchronized (DigestTimedMetadataType.class) {
            return values.computeIfAbsent(value, v -> new DigestTimedMetadataType(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<DigestTimedMetadataTypeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        DigestTimedMetadataType other = (DigestTimedMetadataType) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "DigestTimedMetadataType [value=" + value + "]";
    }

    // return an array just like an enum
    public static DigestTimedMetadataType[] values() {
        synchronized (DigestTimedMetadataType.class) {
            return values.values().toArray(new DigestTimedMetadataType[] {});
        }
    }

    private static final Map<String, DigestTimedMetadataType> createValuesMap() {
        Map<String, DigestTimedMetadataType> map = new LinkedHashMap<>();
        map.put("timed", TIMED);
        return map;
    }

    private static final Map<String, DigestTimedMetadataTypeEnum> createEnumsMap() {
        Map<String, DigestTimedMetadataTypeEnum> map = new HashMap<>();
        map.put("timed", DigestTimedMetadataTypeEnum.TIMED);
        return map;
    }
    
    
    public enum DigestTimedMetadataTypeEnum {

        TIMED("timed"),;

        private final String value;

        private DigestTimedMetadataTypeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

