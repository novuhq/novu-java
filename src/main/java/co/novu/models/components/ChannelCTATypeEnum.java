/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * ChannelCTATypeEnum
 * 
 * <p>Type of call to action
 */
public class ChannelCTATypeEnum {

    public static final ChannelCTATypeEnum REDIRECT = new ChannelCTATypeEnum("redirect");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ChannelCTATypeEnum> values = createValuesMap();
    private static final Map<String, ChannelCTATypeEnumEnum> enums = createEnumsMap();

    private final String value;

    private ChannelCTATypeEnum(String value) {
        this.value = value;
    }

    /**
     * Returns a ChannelCTATypeEnum with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ChannelCTATypeEnum
     */ 
    @JsonCreator
    public static ChannelCTATypeEnum of(String value) {
        synchronized (ChannelCTATypeEnum.class) {
            return values.computeIfAbsent(value, v -> new ChannelCTATypeEnum(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ChannelCTATypeEnumEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ChannelCTATypeEnum other = (ChannelCTATypeEnum) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ChannelCTATypeEnum [value=" + value + "]";
    }

    // return an array just like an enum
    public static ChannelCTATypeEnum[] values() {
        synchronized (ChannelCTATypeEnum.class) {
            return values.values().toArray(new ChannelCTATypeEnum[] {});
        }
    }

    private static final Map<String, ChannelCTATypeEnum> createValuesMap() {
        Map<String, ChannelCTATypeEnum> map = new LinkedHashMap<>();
        map.put("redirect", REDIRECT);
        return map;
    }

    private static final Map<String, ChannelCTATypeEnumEnum> createEnumsMap() {
        Map<String, ChannelCTATypeEnumEnum> map = new HashMap<>();
        map.put("redirect", ChannelCTATypeEnumEnum.REDIRECT);
        return map;
    }
    
    
    public enum ChannelCTATypeEnumEnum {

        REDIRECT("redirect"),;

        private final String value;

        private ChannelCTATypeEnumEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

