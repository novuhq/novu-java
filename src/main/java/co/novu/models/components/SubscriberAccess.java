/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * SubscriberAccess
 * 
 * <p>Controls whether the agent accepts inbound messages from senders not yet linked to a subscriber,
 * across all channels. "open" on managed agents auto-creates a lightweight subscriber so the agent can
 * reply; on custom-code / self-hosted agents, the turn is forwarded to the bridge with a null
 * subscriber. "restricted" rejects unknown senders with a managed denial reply (any runtime).
 * 
 * <p>Optional on update (partial PATCH). Persisted agents always have a value — managed create defaults
 * to "open"; self-hosted create defaults to "restricted".
 */
public class SubscriberAccess {

    public static final SubscriberAccess OPEN = new SubscriberAccess("open");
    public static final SubscriberAccess RESTRICTED = new SubscriberAccess("restricted");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, SubscriberAccess> values = createValuesMap();
    private static final Map<String, SubscriberAccessEnum> enums = createEnumsMap();

    private final String value;

    private SubscriberAccess(String value) {
        this.value = value;
    }

    /**
     * Returns a SubscriberAccess with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as SubscriberAccess
     */ 
    @JsonCreator
    public static SubscriberAccess of(String value) {
        synchronized (SubscriberAccess.class) {
            return values.computeIfAbsent(value, v -> new SubscriberAccess(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<SubscriberAccessEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        SubscriberAccess other = (SubscriberAccess) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "SubscriberAccess [value=" + value + "]";
    }

    // return an array just like an enum
    public static SubscriberAccess[] values() {
        synchronized (SubscriberAccess.class) {
            return values.values().toArray(new SubscriberAccess[] {});
        }
    }

    private static final Map<String, SubscriberAccess> createValuesMap() {
        Map<String, SubscriberAccess> map = new LinkedHashMap<>();
        map.put("open", OPEN);
        map.put("restricted", RESTRICTED);
        return map;
    }

    private static final Map<String, SubscriberAccessEnum> createEnumsMap() {
        Map<String, SubscriberAccessEnum> map = new HashMap<>();
        map.put("open", SubscriberAccessEnum.OPEN);
        map.put("restricted", SubscriberAccessEnum.RESTRICTED);
        return map;
    }
    
    
    public enum SubscriberAccessEnum {

        OPEN("open"),
        RESTRICTED("restricted"),;

        private final String value;

        private SubscriberAccessEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

