/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class TypeTool {

    public static final TypeTool TOOL = new TypeTool("tool");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, TypeTool> values = createValuesMap();
    private static final Map<String, TypeToolEnum> enums = createEnumsMap();

    private final String value;

    private TypeTool(String value) {
        this.value = value;
    }

    /**
     * Returns a TypeTool with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as TypeTool
     */ 
    @JsonCreator
    public static TypeTool of(String value) {
        synchronized (TypeTool.class) {
            return values.computeIfAbsent(value, v -> new TypeTool(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<TypeToolEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        TypeTool other = (TypeTool) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "TypeTool [value=" + value + "]";
    }

    // return an array just like an enum
    public static TypeTool[] values() {
        synchronized (TypeTool.class) {
            return values.values().toArray(new TypeTool[] {});
        }
    }

    private static final Map<String, TypeTool> createValuesMap() {
        Map<String, TypeTool> map = new LinkedHashMap<>();
        map.put("tool", TOOL);
        return map;
    }

    private static final Map<String, TypeToolEnum> createEnumsMap() {
        Map<String, TypeToolEnum> map = new HashMap<>();
        map.put("tool", TypeToolEnum.TOOL);
        return map;
    }
    
    
    public enum TypeToolEnum {

        TOOL("tool"),;

        private final String value;

        private TypeToolEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

