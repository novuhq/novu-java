/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class DelayScheduledMetadataType {

    public static final DelayScheduledMetadataType SCHEDULED = new DelayScheduledMetadataType("scheduled");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, DelayScheduledMetadataType> values = createValuesMap();
    private static final Map<String, DelayScheduledMetadataTypeEnum> enums = createEnumsMap();

    private final String value;

    private DelayScheduledMetadataType(String value) {
        this.value = value;
    }

    /**
     * Returns a DelayScheduledMetadataType with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as DelayScheduledMetadataType
     */ 
    @JsonCreator
    public static DelayScheduledMetadataType of(String value) {
        synchronized (DelayScheduledMetadataType.class) {
            return values.computeIfAbsent(value, v -> new DelayScheduledMetadataType(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<DelayScheduledMetadataTypeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        DelayScheduledMetadataType other = (DelayScheduledMetadataType) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "DelayScheduledMetadataType [value=" + value + "]";
    }

    // return an array just like an enum
    public static DelayScheduledMetadataType[] values() {
        synchronized (DelayScheduledMetadataType.class) {
            return values.values().toArray(new DelayScheduledMetadataType[] {});
        }
    }

    private static final Map<String, DelayScheduledMetadataType> createValuesMap() {
        Map<String, DelayScheduledMetadataType> map = new LinkedHashMap<>();
        map.put("scheduled", SCHEDULED);
        return map;
    }

    private static final Map<String, DelayScheduledMetadataTypeEnum> createEnumsMap() {
        Map<String, DelayScheduledMetadataTypeEnum> map = new HashMap<>();
        map.put("scheduled", DelayScheduledMetadataTypeEnum.SCHEDULED);
        return map;
    }
    
    
    public enum DelayScheduledMetadataTypeEnum {

        SCHEDULED("scheduled"),;

        private final String value;

        private DelayScheduledMetadataTypeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

