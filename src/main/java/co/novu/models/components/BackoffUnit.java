/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class BackoffUnit {

    public static final BackoffUnit SECONDS = new BackoffUnit("seconds");
    public static final BackoffUnit MINUTES = new BackoffUnit("minutes");
    public static final BackoffUnit HOURS = new BackoffUnit("hours");
    public static final BackoffUnit DAYS = new BackoffUnit("days");
    public static final BackoffUnit WEEKS = new BackoffUnit("weeks");
    public static final BackoffUnit MONTHS = new BackoffUnit("months");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, BackoffUnit> values = createValuesMap();
    private static final Map<String, BackoffUnitEnum> enums = createEnumsMap();

    private final String value;

    private BackoffUnit(String value) {
        this.value = value;
    }

    /**
     * Returns a BackoffUnit with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as BackoffUnit
     */ 
    @JsonCreator
    public static BackoffUnit of(String value) {
        synchronized (BackoffUnit.class) {
            return values.computeIfAbsent(value, v -> new BackoffUnit(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<BackoffUnitEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        BackoffUnit other = (BackoffUnit) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "BackoffUnit [value=" + value + "]";
    }

    // return an array just like an enum
    public static BackoffUnit[] values() {
        synchronized (BackoffUnit.class) {
            return values.values().toArray(new BackoffUnit[] {});
        }
    }

    private static final Map<String, BackoffUnit> createValuesMap() {
        Map<String, BackoffUnit> map = new LinkedHashMap<>();
        map.put("seconds", SECONDS);
        map.put("minutes", MINUTES);
        map.put("hours", HOURS);
        map.put("days", DAYS);
        map.put("weeks", WEEKS);
        map.put("months", MONTHS);
        return map;
    }

    private static final Map<String, BackoffUnitEnum> createEnumsMap() {
        Map<String, BackoffUnitEnum> map = new HashMap<>();
        map.put("seconds", BackoffUnitEnum.SECONDS);
        map.put("minutes", BackoffUnitEnum.MINUTES);
        map.put("hours", BackoffUnitEnum.HOURS);
        map.put("days", BackoffUnitEnum.DAYS);
        map.put("weeks", BackoffUnitEnum.WEEKS);
        map.put("months", BackoffUnitEnum.MONTHS);
        return map;
    }
    
    
    public enum BackoffUnitEnum {

        SECONDS("seconds"),
        MINUTES("minutes"),
        HOURS("hours"),
        DAYS("days"),
        WEEKS("weeks"),
        MONTHS("months"),;

        private final String value;

        private BackoffUnitEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

