/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import co.novu.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;


public class ToolWebhookEndpointDto {
    /**
     * Destination webhook URL (often a per-subscriber capability URL). Encrypted at rest on the channel
     * endpoint (`endpoint` field).
     */
    @JsonProperty("url")
    private String url;

    /**
     * Optional headers (e.g. auth tokens) sent with every request to this webhook. Header values are
     * encrypted at rest on the channel endpoint (`endpoint` field).
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("headers")
    private Headers headers;

    /**
     * Optional HTTP method override for this webhook. Defaults to the integration-level method.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("method")
    private Method method;

    @JsonCreator
    public ToolWebhookEndpointDto(
            @JsonProperty("url") @Nonnull String url,
            @JsonProperty("headers") @Nullable Headers headers,
            @JsonProperty("method") @Nullable Method method) {
        this.url = Optional.ofNullable(url)
            .orElseThrow(() -> new IllegalArgumentException("url cannot be null"));
        this.headers = headers;
        this.method = method;
    }
    
    public ToolWebhookEndpointDto(
            @Nonnull String url) {
        this(url, null, null);
    }

    /**
     * Destination webhook URL (often a per-subscriber capability URL). Encrypted at rest on the channel
     * endpoint (`endpoint` field).
     */
    public String url() {
        return this.url;
    }

    /**
     * Optional headers (e.g. auth tokens) sent with every request to this webhook. Header values are
     * encrypted at rest on the channel endpoint (`endpoint` field).
     */
    public Optional<Headers> headers() {
        return Optional.ofNullable(this.headers);
    }

    /**
     * Optional HTTP method override for this webhook. Defaults to the integration-level method.
     */
    public Optional<Method> method() {
        return Optional.ofNullable(this.method);
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * Destination webhook URL (often a per-subscriber capability URL). Encrypted at rest on the channel
     * endpoint (`endpoint` field).
     */
    public ToolWebhookEndpointDto withUrl(@Nonnull String url) {
        this.url = Utils.checkNotNull(url, "url");
        return this;
    }


    /**
     * Optional headers (e.g. auth tokens) sent with every request to this webhook. Header values are
     * encrypted at rest on the channel endpoint (`endpoint` field).
     */
    public ToolWebhookEndpointDto withHeaders(@Nullable Headers headers) {
        this.headers = headers;
        return this;
    }


    /**
     * Optional HTTP method override for this webhook. Defaults to the integration-level method.
     */
    public ToolWebhookEndpointDto withMethod(@Nullable Method method) {
        this.method = method;
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ToolWebhookEndpointDto other = (ToolWebhookEndpointDto) o;
        return 
            Utils.enhancedDeepEquals(this.url, other.url) &&
            Utils.enhancedDeepEquals(this.headers, other.headers) &&
            Utils.enhancedDeepEquals(this.method, other.method);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            url, headers, method);
    }
    
    @Override
    public String toString() {
        return Utils.toString(ToolWebhookEndpointDto.class,
                "url", url,
                "headers", headers,
                "method", method);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String url;

        private Headers headers;

        private Method method;

        private Builder() {
          // force use of static builder() method
        }

        /**
         * Destination webhook URL (often a per-subscriber capability URL). Encrypted at rest on the channel
         * endpoint (`endpoint` field).
         */
        public Builder url(@Nonnull String url) {
            this.url = Utils.checkNotNull(url, "url");
            return this;
        }

        /**
         * Optional headers (e.g. auth tokens) sent with every request to this webhook. Header values are
         * encrypted at rest on the channel endpoint (`endpoint` field).
         */
        public Builder headers(@Nullable Headers headers) {
            this.headers = headers;
            return this;
        }

        /**
         * Optional HTTP method override for this webhook. Defaults to the integration-level method.
         */
        public Builder method(@Nullable Method method) {
            this.method = method;
            return this;
        }

        public ToolWebhookEndpointDto build() {
            return new ToolWebhookEndpointDto(
                url, headers, method);
        }

    }
}
