/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class TypeInApp {

    public static final TypeInApp IN_APP = new TypeInApp("in_app");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, TypeInApp> values = createValuesMap();
    private static final Map<String, TypeInAppEnum> enums = createEnumsMap();

    private final String value;

    private TypeInApp(String value) {
        this.value = value;
    }

    /**
     * Returns a TypeInApp with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as TypeInApp
     */ 
    @JsonCreator
    public static TypeInApp of(String value) {
        synchronized (TypeInApp.class) {
            return values.computeIfAbsent(value, v -> new TypeInApp(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<TypeInAppEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        TypeInApp other = (TypeInApp) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "TypeInApp [value=" + value + "]";
    }

    // return an array just like an enum
    public static TypeInApp[] values() {
        synchronized (TypeInApp.class) {
            return values.values().toArray(new TypeInApp[] {});
        }
    }

    private static final Map<String, TypeInApp> createValuesMap() {
        Map<String, TypeInApp> map = new LinkedHashMap<>();
        map.put("in_app", IN_APP);
        return map;
    }

    private static final Map<String, TypeInAppEnum> createEnumsMap() {
        Map<String, TypeInAppEnum> map = new HashMap<>();
        map.put("in_app", TypeInAppEnum.IN_APP);
        return map;
    }
    
    
    public enum TypeInAppEnum {

        IN_APP("in_app"),;

        private final String value;

        private TypeInAppEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

