/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import co.novu.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonProperty;
import jakarta.annotation.Nonnull;
import java.lang.Override;
import java.lang.String;
import java.util.Map;
import java.util.Optional;


public class WorkflowPreferencesDto {
    /**
     * A preference for the workflow. The values specified here will be used if no preference is specified
     * for a channel.
     */
    @JsonProperty("all")
    private WorkflowPreferencesDtoAll all;

    /**
     * Preferences for different communication channels
     */
    @JsonProperty("channels")
    private Map<String, ChannelPreferenceDto> channels;

    @JsonCreator
    public WorkflowPreferencesDto(
            @JsonProperty("all") @Nonnull WorkflowPreferencesDtoAll all,
            @JsonProperty("channels") @Nonnull Map<String, ChannelPreferenceDto> channels) {
        channels = Utils.emptyMapIfNull(channels);
        this.all = Optional.ofNullable(all)
            .orElseThrow(() -> new IllegalArgumentException("all cannot be null"));
        this.channels = Optional.ofNullable(channels)
            .orElseThrow(() -> new IllegalArgumentException("channels cannot be null"));
    }

    /**
     * A preference for the workflow. The values specified here will be used if no preference is specified
     * for a channel.
     */
    public WorkflowPreferencesDtoAll all() {
        return this.all;
    }

    /**
     * Preferences for different communication channels
     */
    public Map<String, ChannelPreferenceDto> channels() {
        return this.channels;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * A preference for the workflow. The values specified here will be used if no preference is specified
     * for a channel.
     */
    public WorkflowPreferencesDto withAll(@Nonnull WorkflowPreferencesDtoAll all) {
        this.all = Utils.checkNotNull(all, "all");
        return this;
    }


    /**
     * Preferences for different communication channels
     */
    public WorkflowPreferencesDto withChannels(@Nonnull Map<String, ChannelPreferenceDto> channels) {
        this.channels = Utils.checkNotNull(channels, "channels");
        return this;
    }


    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        WorkflowPreferencesDto other = (WorkflowPreferencesDto) o;
        return 
            Utils.enhancedDeepEquals(this.all, other.all) &&
            Utils.enhancedDeepEquals(this.channels, other.channels);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            all, channels);
    }
    
    @Override
    public String toString() {
        return Utils.toString(WorkflowPreferencesDto.class,
                "all", all,
                "channels", channels);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private WorkflowPreferencesDtoAll all;

        private Map<String, ChannelPreferenceDto> channels;

        private Builder() {
          // force use of static builder() method
        }

        /**
         * A preference for the workflow. The values specified here will be used if no preference is specified
         * for a channel.
         */
        public Builder all(@Nonnull WorkflowPreferencesDtoAll all) {
            this.all = Utils.checkNotNull(all, "all");
            return this;
        }

        /**
         * Preferences for different communication channels
         */
        public Builder channels(@Nonnull Map<String, ChannelPreferenceDto> channels) {
            this.channels = Utils.checkNotNull(channels, "channels");
            return this;
        }

        public WorkflowPreferencesDto build() {
            return new WorkflowPreferencesDto(
                all, channels);
        }

    }
}
