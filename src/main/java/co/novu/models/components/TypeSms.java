/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class TypeSms {

    public static final TypeSms SMS = new TypeSms("sms");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, TypeSms> values = createValuesMap();
    private static final Map<String, TypeSmsEnum> enums = createEnumsMap();

    private final String value;

    private TypeSms(String value) {
        this.value = value;
    }

    /**
     * Returns a TypeSms with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as TypeSms
     */ 
    @JsonCreator
    public static TypeSms of(String value) {
        synchronized (TypeSms.class) {
            return values.computeIfAbsent(value, v -> new TypeSms(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<TypeSmsEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        TypeSms other = (TypeSms) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "TypeSms [value=" + value + "]";
    }

    // return an array just like an enum
    public static TypeSms[] values() {
        synchronized (TypeSms.class) {
            return values.values().toArray(new TypeSms[] {});
        }
    }

    private static final Map<String, TypeSms> createValuesMap() {
        Map<String, TypeSms> map = new LinkedHashMap<>();
        map.put("sms", SMS);
        return map;
    }

    private static final Map<String, TypeSmsEnum> createEnumsMap() {
        Map<String, TypeSmsEnum> map = new HashMap<>();
        map.put("sms", TypeSmsEnum.SMS);
        return map;
    }
    
    
    public enum TypeSmsEnum {

        SMS("sms"),;

        private final String value;

        private TypeSmsEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

