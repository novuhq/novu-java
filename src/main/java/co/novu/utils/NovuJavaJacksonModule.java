/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu.utils;

import com.fasterxml.jackson.annotation.JsonAutoDetect;
import com.fasterxml.jackson.annotation.PropertyAccessor;
import com.fasterxml.jackson.core.Version;
import com.fasterxml.jackson.databind.DeserializationFeature;
import com.fasterxml.jackson.databind.Module;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.SerializationFeature;
import org.openapitools.jackson.nullable.JsonNullableModule;
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module;
import com.fasterxml.jackson.datatype.jsr310.JavaTimeModule;

/**
 * A composite Jackson {@link Module} that registers all SDK-required Jackson customizations.
 *
 * <p>Register this with a custom {@link ObjectMapper} to enable full serialization support
 * for SDK types. Bundles {@code JavaTimeModule}, {@code Jdk8Module}, {@code JsonNullableModule},
 * and SDK strict deserializers, and applies the same feature flags used by
 * {@link JSON#getMapper()}:
 * <ul>
 *   <li>Disables {@code WRITE_DATES_AS_TIMESTAMPS} (ISO-8601 date strings)</li>
 *   <li>Disables {@code FAIL_ON_UNKNOWN_PROPERTIES}</li>
 *   <li>Disables {@code FAIL_ON_EMPTY_BEANS}</li>
 *   <li>Enables {@code FAIL_ON_NULL_FOR_PRIMITIVES}</li>
 *   <li>Restricts field visibility to annotated fields only</li>
 * </ul>
 *
 * <pre>{@code
 * ObjectMapper myMapper = new ObjectMapper()
 *     .registerModule(new NovuJavaJacksonModule());
 * String json = myMapper.writeValueAsString(response);
 * }</pre>
 *
 * <p>Alternatively, use {@link JSON#getMapper()} to access the pre-configured SDK mapper directly.
 */
public class NovuJavaJacksonModule extends Module {

    @Override
    public String getModuleName() {
        return "NovuJavaJacksonModule";
    }

    @Override
    public Version version() {
        return Version.unknownVersion();
    }

    @Override
    public void setupModule(SetupContext context) {
        new JavaTimeModule().setupModule(context);
        new Jdk8Module().setupModule(context);
        new JsonNullableModule().setupModule(context);
        Deserializers.STRICT_DESERIALIZERS.setupModule(context);
        if (context.getOwner() instanceof ObjectMapper) {
            ObjectMapper mapper = (ObjectMapper) context.getOwner();
            mapper.configure(DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES, false)
                  .configure(SerializationFeature.FAIL_ON_EMPTY_BEANS, false)
                  .disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS)
                  .enable(DeserializationFeature.FAIL_ON_NULL_FOR_PRIMITIVES)
                  .setVisibility(PropertyAccessor.ALL, JsonAutoDetect.Visibility.NONE);
        }
    }
}
