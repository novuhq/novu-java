/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu;

import static co.novu.operations.Operations.AsyncRequestOperation;

import co.novu.models.components.BulkTriggerEventDto;
import co.novu.models.components.TriggerEventRequestDto;
import co.novu.models.components.TriggerEventToAllRequestDto;
import co.novu.models.operations.EventsControllerBroadcastEventToAllRequest;
import co.novu.models.operations.EventsControllerCancelRequest;
import co.novu.models.operations.EventsControllerTriggerBulkRequest;
import co.novu.models.operations.EventsControllerTriggerRequest;
import co.novu.models.operations.async.EventsControllerBroadcastEventToAllRequestBuilder;
import co.novu.models.operations.async.EventsControllerBroadcastEventToAllResponse;
import co.novu.models.operations.async.EventsControllerCancelRequestBuilder;
import co.novu.models.operations.async.EventsControllerCancelResponse;
import co.novu.models.operations.async.EventsControllerTriggerBulkRequestBuilder;
import co.novu.models.operations.async.EventsControllerTriggerBulkResponse;
import co.novu.models.operations.async.EventsControllerTriggerRequestBuilder;
import co.novu.models.operations.async.EventsControllerTriggerResponse;
import co.novu.operations.EventsControllerBroadcastEventToAll;
import co.novu.operations.EventsControllerCancel;
import co.novu.operations.EventsControllerTrigger;
import co.novu.operations.EventsControllerTriggerBulk;
import co.novu.utils.Headers;
import co.novu.utils.Options;
import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.String;
import java.util.concurrent.CompletableFuture;

/**
 * Novu API: Novu REST API. Please see https://docs.novu.co/api-reference for more details.
 * 
 * <p><a href="https://docs.novu.co">Novu Documentation</a>
 */
public class AsyncNovu {
    private static final Headers _headers = Headers.EMPTY;

    private final AsyncContexts contexts;
    /**
     * Environments allow you to manage different stages of your application development lifecycle. Each
     * environment has its own set of API keys and configurations, enabling you to separate development,
     * staging, and production workflows.
     * 
     * <p><a href="https://docs.novu.co/platform/environments">https://docs.novu.co/platform/environments</a>
     */
    private final AsyncEnvironments environments;

    private final AsyncActivity activity;
    /**
     * Layouts are reusable wrappers for your email notifications.
     * 
     * <p><a href="https://docs.novu.co/platform/workflow/layouts">https://docs.novu.co/platform/workflow/layouts</a>
     */
    private final AsyncLayouts layouts;
    /**
     * A subscriber in Novu represents someone who should receive a message. A subscriber's profile
     * information contains important attributes about the subscriber that will be used in messages (name,
     * email). The subscriber object can contain other key-value pairs that can be used to further
     * personalize your messages.
     * 
     * <p><a href="https://docs.novu.co/subscribers/subscribers">https://docs.novu.co/subscribers/subscribers</a>
     */
    private final AsyncSubscribers subscribers;

    private final AsyncSubscribersTopics subscribersTopics;
    /**
     * Topics are a way to group subscribers together so that they can be notified of events at once. A
     * topic is identified by a custom key. This can be helpful for things like sending out marketing
     * emails or notifying users of new features.
     * 
     * <p>Topics can also be used to send notifications to the subscribers who have been grouped together
     * based on their interests, location, activities and much more.
     * 
     * <p><a href="https://docs.novu.co/subscribers/topics">https://docs.novu.co/subscribers/topics</a>
     */
    private final AsyncTopics topics;
    /**
     * Used to localize your notifications to different languages.
     * 
     * <p><a href="https://docs.novu.co/platform/workflow/advanced-features/translations">https://docs.novu.co/platform/workflow/advanced-features/translations</a>
     */
    private final AsyncTranslations translations;
    /**
     * All notifications are sent via a workflow. Each workflow acts as a container for the logic and
     * blueprint that are associated with a type of notification in your system.
     * 
     * <p><a href="https://docs.novu.co/workflows">https://docs.novu.co/workflows</a>
     */
    private final AsyncWorkflows workflows;

    private final AsyncChannelConnections channelConnections;

    private final AsyncChannelEndpoints channelEndpoints;
    /**
     * With the help of the Integration Store, you can easily integrate your favorite delivery provider.
     * During the runtime of the API, the Integrations Store is responsible for storing the configurations
     * of all the providers.
     * 
     * <p><a href="https://docs.novu.co/platform/integrations/overview">https://docs.novu.co/platform/integrations/overview</a>
     */
    private final AsyncIntegrations integrations;
    /**
     * A message in Novu represents a notification delivered to a recipient on a particular channel.
     * Messages contain information about the request that triggered its delivery, a view of the data sent
     * to the recipient, and a timeline of its lifecycle events. Learn more about messages.
     * 
     * <p><a href="https://docs.novu.co/workflows/messages">https://docs.novu.co/workflows/messages</a>
     */
    private final AsyncMessages messages;

    private final AsyncNotifications notifications;

    private final AsyncSubscribersMessages2 subscribersMessages;

    private final AsyncSubscribersNotifications1 subscribersNotifications;

    private final AsyncSubscribersProperties subscribersProperties;

    public AsyncContexts contexts() {
        return contexts;
    }
    /**
     * Environments allow you to manage different stages of your application development lifecycle. Each
     * environment has its own set of API keys and configurations, enabling you to separate development,
     * staging, and production workflows.
     * 
     * <p><a href="https://docs.novu.co/platform/environments">https://docs.novu.co/platform/environments</a>
     */
    public AsyncEnvironments environments() {
        return environments;
    }

    public AsyncActivity activity() {
        return activity;
    }
    /**
     * Layouts are reusable wrappers for your email notifications.
     * 
     * <p><a href="https://docs.novu.co/platform/workflow/layouts">https://docs.novu.co/platform/workflow/layouts</a>
     */
    public AsyncLayouts layouts() {
        return layouts;
    }
    /**
     * A subscriber in Novu represents someone who should receive a message. A subscriber's profile
     * information contains important attributes about the subscriber that will be used in messages (name,
     * email). The subscriber object can contain other key-value pairs that can be used to further
     * personalize your messages.
     * 
     * <p><a href="https://docs.novu.co/subscribers/subscribers">https://docs.novu.co/subscribers/subscribers</a>
     */
    public AsyncSubscribers subscribers() {
        return subscribers;
    }

    public AsyncSubscribersTopics subscribersTopics() {
        return subscribersTopics;
    }
    /**
     * Topics are a way to group subscribers together so that they can be notified of events at once. A
     * topic is identified by a custom key. This can be helpful for things like sending out marketing
     * emails or notifying users of new features.
     * 
     * <p>Topics can also be used to send notifications to the subscribers who have been grouped together
     * based on their interests, location, activities and much more.
     * 
     * <p><a href="https://docs.novu.co/subscribers/topics">https://docs.novu.co/subscribers/topics</a>
     */
    public AsyncTopics topics() {
        return topics;
    }
    /**
     * Used to localize your notifications to different languages.
     * 
     * <p><a href="https://docs.novu.co/platform/workflow/advanced-features/translations">https://docs.novu.co/platform/workflow/advanced-features/translations</a>
     */
    public AsyncTranslations translations() {
        return translations;
    }
    /**
     * All notifications are sent via a workflow. Each workflow acts as a container for the logic and
     * blueprint that are associated with a type of notification in your system.
     * 
     * <p><a href="https://docs.novu.co/workflows">https://docs.novu.co/workflows</a>
     */
    public AsyncWorkflows workflows() {
        return workflows;
    }

    public AsyncChannelConnections channelConnections() {
        return channelConnections;
    }

    public AsyncChannelEndpoints channelEndpoints() {
        return channelEndpoints;
    }
    /**
     * With the help of the Integration Store, you can easily integrate your favorite delivery provider.
     * During the runtime of the API, the Integrations Store is responsible for storing the configurations
     * of all the providers.
     * 
     * <p><a href="https://docs.novu.co/platform/integrations/overview">https://docs.novu.co/platform/integrations/overview</a>
     */
    public AsyncIntegrations integrations() {
        return integrations;
    }
    /**
     * A message in Novu represents a notification delivered to a recipient on a particular channel.
     * Messages contain information about the request that triggered its delivery, a view of the data sent
     * to the recipient, and a timeline of its lifecycle events. Learn more about messages.
     * 
     * <p><a href="https://docs.novu.co/workflows/messages">https://docs.novu.co/workflows/messages</a>
     */
    public AsyncMessages messages() {
        return messages;
    }

    public AsyncNotifications notifications() {
        return notifications;
    }

    public AsyncSubscribersMessages2 subscribersMessages() {
        return subscribersMessages;
    }

    public AsyncSubscribersNotifications1 subscribersNotifications() {
        return subscribersNotifications;
    }

    public AsyncSubscribersProperties subscribersProperties() {
        return subscribersProperties;
    }

    private SDKConfiguration sdkConfiguration;
    private final Novu syncSDK;

    AsyncNovu(Novu syncSDK, SDKConfiguration sdkConfiguration) {
        this.syncSDK = syncSDK;
        this.sdkConfiguration = sdkConfiguration;
        this.contexts = new AsyncContexts(syncSDK.contexts(), sdkConfiguration);
        this.environments = new AsyncEnvironments(syncSDK.environments(), sdkConfiguration);
        this.activity = new AsyncActivity(syncSDK.activity(), sdkConfiguration);
        this.layouts = new AsyncLayouts(syncSDK.layouts(), sdkConfiguration);
        this.subscribers = new AsyncSubscribers(syncSDK.subscribers(), sdkConfiguration);
        this.subscribersTopics = new AsyncSubscribersTopics(syncSDK.subscribersTopics(), sdkConfiguration);
        this.topics = new AsyncTopics(syncSDK.topics(), sdkConfiguration);
        this.translations = new AsyncTranslations(syncSDK.translations(), sdkConfiguration);
        this.workflows = new AsyncWorkflows(syncSDK.workflows(), sdkConfiguration);
        this.channelConnections = new AsyncChannelConnections(syncSDK.channelConnections(), sdkConfiguration);
        this.channelEndpoints = new AsyncChannelEndpoints(syncSDK.channelEndpoints(), sdkConfiguration);
        this.integrations = new AsyncIntegrations(syncSDK.integrations(), sdkConfiguration);
        this.messages = new AsyncMessages(syncSDK.messages(), sdkConfiguration);
        this.notifications = new AsyncNotifications(syncSDK.notifications(), sdkConfiguration);
        this.subscribersMessages = new AsyncSubscribersMessages2(syncSDK.subscribersMessages(), sdkConfiguration);
        this.subscribersNotifications = new AsyncSubscribersNotifications1(syncSDK.subscribersNotifications(), sdkConfiguration);
        this.subscribersProperties = new AsyncSubscribersProperties(syncSDK.subscribersProperties(), sdkConfiguration);
    }

    /**
     * Switches to the sync SDK.
     * 
     * @return The sync SDK
     */
    public Novu sync() {
        return syncSDK;
    }
    /**
     * Trigger event
     * 
     * <p>Trigger event is the main (and only) way to send notifications to subscribers. The trigger
     * identifier is used to match the particular workflow associated with it. Maximum number of recipients
     * can be 100.
     * 
     * <p>Additional information can be passed according the body interface below.
     * To prevent duplicate triggers, you can optionally pass a **transactionId** in the request body. If
     * the same **transactionId** is used again, the trigger will be ignored. The retention period depends
     * on your billing tier.
     * 
     * @return The async call builder
     */
    public EventsControllerTriggerRequestBuilder trigger() {
        return new EventsControllerTriggerRequestBuilder(sdkConfiguration);
    }

    /**
     * Trigger event
     * 
     * <p>Trigger event is the main (and only) way to send notifications to subscribers. The trigger
     * identifier is used to match the particular workflow associated with it. Maximum number of recipients
     * can be 100.
     * 
     * <p>Additional information can be passed according the body interface below.
     * To prevent duplicate triggers, you can optionally pass a **transactionId** in the request body. If
     * the same **transactionId** is used again, the trigger will be ignored. The retention period depends
     * on your billing tier.
     * 
     * @param body 
     * @return {@code CompletableFuture<EventsControllerTriggerResponse>} - The async response
     */
    public CompletableFuture<EventsControllerTriggerResponse> trigger(@Nonnull TriggerEventRequestDto body) {
        return trigger(null, body, null);
    }

    /**
     * Trigger event
     * 
     * <p>Trigger event is the main (and only) way to send notifications to subscribers. The trigger
     * identifier is used to match the particular workflow associated with it. Maximum number of recipients
     * can be 100.
     * 
     * <p>Additional information can be passed according the body interface below.
     * To prevent duplicate triggers, you can optionally pass a **transactionId** in the request body. If
     * the same **transactionId** is used again, the trigger will be ignored. The retention period depends
     * on your billing tier.
     * 
     * @param idempotencyKey A header for idempotency purposes
     * @param body 
     * @param options additional options
     * @return {@code CompletableFuture<EventsControllerTriggerResponse>} - The async response
     */
    public CompletableFuture<EventsControllerTriggerResponse> trigger(
            @Nullable String idempotencyKey, @Nonnull TriggerEventRequestDto body,
            @Nullable Options options) {
        EventsControllerTriggerRequest request = new EventsControllerTriggerRequest(idempotencyKey, body);
        AsyncRequestOperation<EventsControllerTriggerRequest, EventsControllerTriggerResponse> operation
              = new EventsControllerTrigger.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }

    /**
     * Cancel triggered event
     * 
     * <p>Using a previously generated transactionId during the event trigger,
     * will cancel any active or pending workflows. This is useful to cancel active digests, delays etc...
     * 
     * @return The async call builder
     */
    public EventsControllerCancelRequestBuilder cancel() {
        return new EventsControllerCancelRequestBuilder(sdkConfiguration);
    }

    /**
     * Cancel triggered event
     * 
     * <p>Using a previously generated transactionId during the event trigger,
     * will cancel any active or pending workflows. This is useful to cancel active digests, delays etc...
     * 
     * @param transactionId 
     * @return {@code CompletableFuture<EventsControllerCancelResponse>} - The async response
     */
    public CompletableFuture<EventsControllerCancelResponse> cancel(@Nonnull String transactionId) {
        return cancel(transactionId, null, null);
    }

    /**
     * Cancel triggered event
     * 
     * <p>Using a previously generated transactionId during the event trigger,
     * will cancel any active or pending workflows. This is useful to cancel active digests, delays etc...
     * 
     * @param transactionId 
     * @param idempotencyKey A header for idempotency purposes
     * @param options additional options
     * @return {@code CompletableFuture<EventsControllerCancelResponse>} - The async response
     */
    public CompletableFuture<EventsControllerCancelResponse> cancel(
            @Nonnull String transactionId, @Nullable String idempotencyKey,
            @Nullable Options options) {
        EventsControllerCancelRequest request = new EventsControllerCancelRequest(transactionId, idempotencyKey);
        AsyncRequestOperation<EventsControllerCancelRequest, EventsControllerCancelResponse> operation
              = new EventsControllerCancel.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }

    /**
     * Broadcast event to all
     * 
     * <p>Trigger a broadcast event to all existing subscribers, could be used to send announcements, etc.
     * 
     * <p>In the future could be used to trigger events to a subset of subscribers based on defined filters.
     * 
     * @return The async call builder
     */
    public EventsControllerBroadcastEventToAllRequestBuilder broadcast() {
        return new EventsControllerBroadcastEventToAllRequestBuilder(sdkConfiguration);
    }

    /**
     * Broadcast event to all
     * 
     * <p>Trigger a broadcast event to all existing subscribers, could be used to send announcements, etc.
     * 
     * <p>In the future could be used to trigger events to a subset of subscribers based on defined filters.
     * 
     * @param body 
     * @return {@code CompletableFuture<EventsControllerBroadcastEventToAllResponse>} - The async response
     */
    public CompletableFuture<EventsControllerBroadcastEventToAllResponse> broadcast(@Nonnull TriggerEventToAllRequestDto body) {
        return broadcast(null, body, null);
    }

    /**
     * Broadcast event to all
     * 
     * <p>Trigger a broadcast event to all existing subscribers, could be used to send announcements, etc.
     * 
     * <p>In the future could be used to trigger events to a subset of subscribers based on defined filters.
     * 
     * @param idempotencyKey A header for idempotency purposes
     * @param body 
     * @param options additional options
     * @return {@code CompletableFuture<EventsControllerBroadcastEventToAllResponse>} - The async response
     */
    public CompletableFuture<EventsControllerBroadcastEventToAllResponse> broadcast(
            @Nullable String idempotencyKey, @Nonnull TriggerEventToAllRequestDto body,
            @Nullable Options options) {
        EventsControllerBroadcastEventToAllRequest request = new EventsControllerBroadcastEventToAllRequest(idempotencyKey, body);
        AsyncRequestOperation<EventsControllerBroadcastEventToAllRequest, EventsControllerBroadcastEventToAllResponse> operation
              = new EventsControllerBroadcastEventToAll.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }

    /**
     * Bulk trigger event
     * 
     * <p>Using this endpoint you can trigger multiple events at once, to avoid multiple calls to the API.
     * The bulk API is limited to 100 events per request.
     * 
     * @return The async call builder
     */
    public EventsControllerTriggerBulkRequestBuilder triggerBulk() {
        return new EventsControllerTriggerBulkRequestBuilder(sdkConfiguration);
    }

    /**
     * Bulk trigger event
     * 
     * <p>Using this endpoint you can trigger multiple events at once, to avoid multiple calls to the API.
     * The bulk API is limited to 100 events per request.
     * 
     * @param body 
     * @return {@code CompletableFuture<EventsControllerTriggerBulkResponse>} - The async response
     */
    public CompletableFuture<EventsControllerTriggerBulkResponse> triggerBulk(@Nonnull BulkTriggerEventDto body) {
        return triggerBulk(null, body, null);
    }

    /**
     * Bulk trigger event
     * 
     * <p>Using this endpoint you can trigger multiple events at once, to avoid multiple calls to the API.
     * The bulk API is limited to 100 events per request.
     * 
     * @param idempotencyKey A header for idempotency purposes
     * @param body 
     * @param options additional options
     * @return {@code CompletableFuture<EventsControllerTriggerBulkResponse>} - The async response
     */
    public CompletableFuture<EventsControllerTriggerBulkResponse> triggerBulk(
            @Nullable String idempotencyKey, @Nonnull BulkTriggerEventDto body,
            @Nullable Options options) {
        EventsControllerTriggerBulkRequest request = new EventsControllerTriggerBulkRequest(idempotencyKey, body);
        AsyncRequestOperation<EventsControllerTriggerBulkRequest, EventsControllerTriggerBulkResponse> operation
              = new EventsControllerTriggerBulk.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }

}
