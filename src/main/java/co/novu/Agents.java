/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu;

import static co.novu.operations.Operations.RequestOperation;

import co.novu.models.components.AgentReplyPayloadDto;
import co.novu.models.components.CreateAgentRequestDto;
import co.novu.models.components.UpdateAgentBridgeRequestDto;
import co.novu.models.components.UpdateAgentRequestDto;
import co.novu.models.operations.AgentReplyControllerHandleAgentReplyHandlerRequest;
import co.novu.models.operations.AgentReplyControllerHandleAgentReplyHandlerRequestBuilder;
import co.novu.models.operations.AgentReplyControllerHandleAgentReplyHandlerResponse;
import co.novu.models.operations.AgentsControllerCreateAgentRequest;
import co.novu.models.operations.AgentsControllerCreateAgentRequestBuilder;
import co.novu.models.operations.AgentsControllerCreateAgentResponse;
import co.novu.models.operations.AgentsControllerDeleteAgentRequest;
import co.novu.models.operations.AgentsControllerDeleteAgentRequestBuilder;
import co.novu.models.operations.AgentsControllerDeleteAgentResponse;
import co.novu.models.operations.AgentsControllerGetAgentRequest;
import co.novu.models.operations.AgentsControllerGetAgentRequestBuilder;
import co.novu.models.operations.AgentsControllerGetAgentResponse;
import co.novu.models.operations.AgentsControllerListAgentsRequest;
import co.novu.models.operations.AgentsControllerListAgentsRequestBuilder;
import co.novu.models.operations.AgentsControllerListAgentsResponse;
import co.novu.models.operations.AgentsControllerUpdateAgentBridgeRequest;
import co.novu.models.operations.AgentsControllerUpdateAgentBridgeRequestBuilder;
import co.novu.models.operations.AgentsControllerUpdateAgentBridgeResponse;
import co.novu.models.operations.AgentsControllerUpdateAgentRequest;
import co.novu.models.operations.AgentsControllerUpdateAgentRequestBuilder;
import co.novu.models.operations.AgentsControllerUpdateAgentResponse;
import co.novu.operations.AgentReplyControllerHandleAgentReplyHandler;
import co.novu.operations.AgentsControllerCreateAgent;
import co.novu.operations.AgentsControllerDeleteAgent;
import co.novu.operations.AgentsControllerGetAgent;
import co.novu.operations.AgentsControllerListAgents;
import co.novu.operations.AgentsControllerUpdateAgent;
import co.novu.operations.AgentsControllerUpdateAgentBridge;
import co.novu.utils.Headers;
import co.novu.utils.Options;
import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.String;

/**
 * Agents are conversational assistants that receive inbound messages from connected channels and
 * respond through a custom code bridge or a managed runtime provider.
 * 
 * <p><a href="https://docs.novu.co/agents">https://docs.novu.co/agents</a>
 */
public class Agents {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncAgents asyncSDK;
    private final AgentsIntegrations integrations;

    Agents(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.integrations = new AgentsIntegrations(this.sdkConfiguration);
        this.asyncSDK = new AsyncAgents(this, sdkConfiguration);
    }

    public final AgentsIntegrations integrations() {
        return integrations;
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncAgents async() {
        return asyncSDK;
    }

    /**
     * Create an agent
     * 
     * <p>Create an agent scoped to the current environment. The identifier must be unique per environment.
     * Set `runtime` to `managed` and supply `managedRuntime` to provision a provider-hosted agent brain.
     * 
     * @return The call builder
     */
    public AgentsControllerCreateAgentRequestBuilder create() {
        return new AgentsControllerCreateAgentRequestBuilder(sdkConfiguration);
    }

    /**
     * Create an agent
     * 
     * <p>Create an agent scoped to the current environment. The identifier must be unique per environment.
     * Set `runtime` to `managed` and supply `managedRuntime` to provision a provider-hosted agent brain.
     * 
     * @param novuAnalyticsSource 
     * @param body 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public AgentsControllerCreateAgentResponse create(@Nonnull String novuAnalyticsSource, @Nonnull CreateAgentRequestDto body) {
        return create(novuAnalyticsSource, null, body,
            null);
    }

    /**
     * Create an agent
     * 
     * <p>Create an agent scoped to the current environment. The identifier must be unique per environment.
     * Set `runtime` to `managed` and supply `managedRuntime` to provision a provider-hosted agent brain.
     * 
     * @param novuAnalyticsSource 
     * @param idempotencyKey A header for idempotency purposes
     * @param body 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public AgentsControllerCreateAgentResponse create(
            @Nonnull String novuAnalyticsSource, @Nullable String idempotencyKey,
            @Nonnull CreateAgentRequestDto body, @Nullable Options options) {
        AgentsControllerCreateAgentRequest request = new AgentsControllerCreateAgentRequest(novuAnalyticsSource, idempotencyKey, body);
        RequestOperation<AgentsControllerCreateAgentRequest, AgentsControllerCreateAgentResponse> operation
              = new AgentsControllerCreateAgent.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * List all agents
     * 
     * <p>Retrieve a cursor-paginated list of agents for the current environment. Use **after**, **before**,
     * **limit**, **orderBy**, and **orderDirection** query parameters.
     * 
     * @return The call builder
     */
    public AgentsControllerListAgentsRequestBuilder list() {
        return new AgentsControllerListAgentsRequestBuilder(sdkConfiguration);
    }

    /**
     * List all agents
     * 
     * <p>Retrieve a cursor-paginated list of agents for the current environment. Use **after**, **before**,
     * **limit**, **orderBy**, and **orderDirection** query parameters.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public AgentsControllerListAgentsResponse list(@Nonnull AgentsControllerListAgentsRequest request) {
        return list(request, null);
    }

    /**
     * List all agents
     * 
     * <p>Retrieve a cursor-paginated list of agents for the current environment. Use **after**, **before**,
     * **limit**, **orderBy**, and **orderDirection** query parameters.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public AgentsControllerListAgentsResponse list(@Nonnull AgentsControllerListAgentsRequest request, @Nullable Options options) {
        RequestOperation<AgentsControllerListAgentsRequest, AgentsControllerListAgentsResponse> operation
              = new AgentsControllerListAgents.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Send an agent reply
     * 
     * <p>Send a message or side-effect into an existing agent conversation from your backend.
     * 
     * <p>Use this endpoint when you are not using `@novu/framework` (for example Python, Go, PHP, .NET, or
     * Java SDKs),
     * or when a server process outside the bridge needs to post into a live conversation.
     * 
     * <p>**Message actions**
     * - `reply` — markdown, interactive card, or tool-approval card (optional `files`)
     * - `edit` — update a previously delivered message in place
     * - `deleteMessages` — remove rendered platform messages (history is kept)
     * - `addReactions` — add emoji reactions to existing messages
     * 
     * <p>**Turn control**
     * - `typing` — `{ status?: string }` to set status, or `"stop"` to clear
     * - `resolve` — mark the conversation resolved (optionally with a final reply)
     * - `error: true` — report a customer-runtime failure (cannot combine with other actions)
     * 
     * <p>**Signals &amp; tools**
     * - `signals` — metadata set/delete/clear, or trigger a Novu workflow
     * - `toolResults` — persist tool outputs into conversation history
     * - `toolApprovalRequest` — ledger a gated tool call (pair with an approval card reply)
     * 
     * <p>Returns `{ data: { messageId, platformThreadId } }` when a reply or edit is delivered;
     * otherwise `{ data: null }`.
     * 
     * @return The call builder
     */
    public AgentReplyControllerHandleAgentReplyHandlerRequestBuilder sendReply() {
        return new AgentReplyControllerHandleAgentReplyHandlerRequestBuilder(sdkConfiguration);
    }

    /**
     * Send an agent reply
     * 
     * <p>Send a message or side-effect into an existing agent conversation from your backend.
     * 
     * <p>Use this endpoint when you are not using `@novu/framework` (for example Python, Go, PHP, .NET, or
     * Java SDKs),
     * or when a server process outside the bridge needs to post into a live conversation.
     * 
     * <p>**Message actions**
     * - `reply` — markdown, interactive card, or tool-approval card (optional `files`)
     * - `edit` — update a previously delivered message in place
     * - `deleteMessages` — remove rendered platform messages (history is kept)
     * - `addReactions` — add emoji reactions to existing messages
     * 
     * <p>**Turn control**
     * - `typing` — `{ status?: string }` to set status, or `"stop"` to clear
     * - `resolve` — mark the conversation resolved (optionally with a final reply)
     * - `error: true` — report a customer-runtime failure (cannot combine with other actions)
     * 
     * <p>**Signals &amp; tools**
     * - `signals` — metadata set/delete/clear, or trigger a Novu workflow
     * - `toolResults` — persist tool outputs into conversation history
     * - `toolApprovalRequest` — ledger a gated tool call (pair with an approval card reply)
     * 
     * <p>Returns `{ data: { messageId, platformThreadId } }` when a reply or edit is delivered;
     * otherwise `{ data: null }`.
     * 
     * @param agentId Agent identifier (slug) for the agent that owns the conversation.
     * @param body 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public AgentReplyControllerHandleAgentReplyHandlerResponse sendReply(@Nonnull String agentId, @Nonnull AgentReplyPayloadDto body) {
        return sendReply(agentId, null, body,
            null);
    }

    /**
     * Send an agent reply
     * 
     * <p>Send a message or side-effect into an existing agent conversation from your backend.
     * 
     * <p>Use this endpoint when you are not using `@novu/framework` (for example Python, Go, PHP, .NET, or
     * Java SDKs),
     * or when a server process outside the bridge needs to post into a live conversation.
     * 
     * <p>**Message actions**
     * - `reply` — markdown, interactive card, or tool-approval card (optional `files`)
     * - `edit` — update a previously delivered message in place
     * - `deleteMessages` — remove rendered platform messages (history is kept)
     * - `addReactions` — add emoji reactions to existing messages
     * 
     * <p>**Turn control**
     * - `typing` — `{ status?: string }` to set status, or `"stop"` to clear
     * - `resolve` — mark the conversation resolved (optionally with a final reply)
     * - `error: true` — report a customer-runtime failure (cannot combine with other actions)
     * 
     * <p>**Signals &amp; tools**
     * - `signals` — metadata set/delete/clear, or trigger a Novu workflow
     * - `toolResults` — persist tool outputs into conversation history
     * - `toolApprovalRequest` — ledger a gated tool call (pair with an approval card reply)
     * 
     * <p>Returns `{ data: { messageId, platformThreadId } }` when a reply or edit is delivered;
     * otherwise `{ data: null }`.
     * 
     * @param agentId Agent identifier (slug) for the agent that owns the conversation.
     * @param idempotencyKey A header for idempotency purposes
     * @param body 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public AgentReplyControllerHandleAgentReplyHandlerResponse sendReply(
            @Nonnull String agentId, @Nullable String idempotencyKey,
            @Nonnull AgentReplyPayloadDto body, @Nullable Options options) {
        AgentReplyControllerHandleAgentReplyHandlerRequest request = new AgentReplyControllerHandleAgentReplyHandlerRequest(agentId, idempotencyKey, body);
        RequestOperation<AgentReplyControllerHandleAgentReplyHandlerRequest, AgentReplyControllerHandleAgentReplyHandlerResponse> operation
              = new AgentReplyControllerHandleAgentReplyHandler.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Retrieve an agent
     * 
     * <p>Retrieve an agent by its external identifier (not the internal MongoDB id).
     * 
     * @return The call builder
     */
    public AgentsControllerGetAgentRequestBuilder retrieve() {
        return new AgentsControllerGetAgentRequestBuilder(sdkConfiguration);
    }

    /**
     * Retrieve an agent
     * 
     * <p>Retrieve an agent by its external identifier (not the internal MongoDB id).
     * 
     * @param identifier 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public AgentsControllerGetAgentResponse retrieve(@Nonnull String identifier) {
        return retrieve(identifier, null, null);
    }

    /**
     * Retrieve an agent
     * 
     * <p>Retrieve an agent by its external identifier (not the internal MongoDB id).
     * 
     * @param identifier 
     * @param idempotencyKey A header for idempotency purposes
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public AgentsControllerGetAgentResponse retrieve(
            @Nonnull String identifier, @Nullable String idempotencyKey,
            @Nullable Options options) {
        AgentsControllerGetAgentRequest request = new AgentsControllerGetAgentRequest(identifier, idempotencyKey);
        RequestOperation<AgentsControllerGetAgentRequest, AgentsControllerGetAgentResponse> operation
              = new AgentsControllerGetAgent.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Update an agent
     * 
     * <p>Update an agent by its external identifier.
     * 
     * @return The call builder
     */
    public AgentsControllerUpdateAgentRequestBuilder update() {
        return new AgentsControllerUpdateAgentRequestBuilder(sdkConfiguration);
    }

    /**
     * Update an agent
     * 
     * <p>Update an agent by its external identifier.
     * 
     * @param identifier 
     * @param body 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public AgentsControllerUpdateAgentResponse update(@Nonnull String identifier, @Nonnull UpdateAgentRequestDto body) {
        return update(identifier, null, body,
            null);
    }

    /**
     * Update an agent
     * 
     * <p>Update an agent by its external identifier.
     * 
     * @param identifier 
     * @param idempotencyKey A header for idempotency purposes
     * @param body 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public AgentsControllerUpdateAgentResponse update(
            @Nonnull String identifier, @Nullable String idempotencyKey,
            @Nonnull UpdateAgentRequestDto body, @Nullable Options options) {
        AgentsControllerUpdateAgentRequest request = new AgentsControllerUpdateAgentRequest(identifier, idempotencyKey, body);
        RequestOperation<AgentsControllerUpdateAgentRequest, AgentsControllerUpdateAgentResponse> operation
              = new AgentsControllerUpdateAgent.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Delete an agent
     * 
     * <p>Delete an agent by identifier, remove all agent-integration links, and clear the agent assignment
     * from any workflows that reference it. For managed-runtime agents, pass `deleteFromProvider=true` to
     * also archive the agent on the provider side (e.g. Anthropic).
     * 
     * <p>By default only the Novu record is deleted and the provider agent is left intact.
     * 
     * @return The call builder
     */
    public AgentsControllerDeleteAgentRequestBuilder delete() {
        return new AgentsControllerDeleteAgentRequestBuilder(sdkConfiguration);
    }

    /**
     * Delete an agent
     * 
     * <p>Delete an agent by identifier, remove all agent-integration links, and clear the agent assignment
     * from any workflows that reference it. For managed-runtime agents, pass `deleteFromProvider=true` to
     * also archive the agent on the provider side (e.g. Anthropic).
     * 
     * <p>By default only the Novu record is deleted and the provider agent is left intact.
     * 
     * @param identifier 
     * @param deleteFromProvider 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public AgentsControllerDeleteAgentResponse delete(@Nonnull String identifier, @Nonnull String deleteFromProvider) {
        return delete(identifier, deleteFromProvider, null,
            null);
    }

    /**
     * Delete an agent
     * 
     * <p>Delete an agent by identifier, remove all agent-integration links, and clear the agent assignment
     * from any workflows that reference it. For managed-runtime agents, pass `deleteFromProvider=true` to
     * also archive the agent on the provider side (e.g. Anthropic).
     * 
     * <p>By default only the Novu record is deleted and the provider agent is left intact.
     * 
     * @param identifier 
     * @param deleteFromProvider 
     * @param idempotencyKey A header for idempotency purposes
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public AgentsControllerDeleteAgentResponse delete(
            @Nonnull String identifier, @Nonnull String deleteFromProvider,
            @Nullable String idempotencyKey, @Nullable Options options) {
        AgentsControllerDeleteAgentRequest request = new AgentsControllerDeleteAgentRequest(identifier, deleteFromProvider, idempotencyKey);
        RequestOperation<AgentsControllerDeleteAgentRequest, AgentsControllerDeleteAgentResponse> operation
              = new AgentsControllerDeleteAgent.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Update an agent bridge
     * 
     * <p>Update the bridge URL configuration for an agent. Used by the CLI to register dev tunnel URLs.
     * Refuses to activate dev bridges on production environments.
     * 
     * @return The call builder
     */
    public AgentsControllerUpdateAgentBridgeRequestBuilder updateBridge() {
        return new AgentsControllerUpdateAgentBridgeRequestBuilder(sdkConfiguration);
    }

    /**
     * Update an agent bridge
     * 
     * <p>Update the bridge URL configuration for an agent. Used by the CLI to register dev tunnel URLs.
     * Refuses to activate dev bridges on production environments.
     * 
     * @param identifier 
     * @param body 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public AgentsControllerUpdateAgentBridgeResponse updateBridge(@Nonnull String identifier, @Nonnull UpdateAgentBridgeRequestDto body) {
        return updateBridge(identifier, null, body,
            null);
    }

    /**
     * Update an agent bridge
     * 
     * <p>Update the bridge URL configuration for an agent. Used by the CLI to register dev tunnel URLs.
     * Refuses to activate dev bridges on production environments.
     * 
     * @param identifier 
     * @param idempotencyKey A header for idempotency purposes
     * @param body 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public AgentsControllerUpdateAgentBridgeResponse updateBridge(
            @Nonnull String identifier, @Nullable String idempotencyKey,
            @Nonnull UpdateAgentBridgeRequestDto body, @Nullable Options options) {
        AgentsControllerUpdateAgentBridgeRequest request = new AgentsControllerUpdateAgentBridgeRequest(identifier, idempotencyKey, body);
        RequestOperation<AgentsControllerUpdateAgentBridgeRequest, AgentsControllerUpdateAgentBridgeResponse> operation
              = new AgentsControllerUpdateAgentBridge.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
