/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package co.novu;

import static co.novu.operations.Operations.RequestOperation;

import co.novu.models.components.CreateContextRequestDto;
import co.novu.models.components.UpdateContextRequestDto;
import co.novu.models.operations.ContextsControllerCreateContextRequest;
import co.novu.models.operations.ContextsControllerCreateContextRequestBuilder;
import co.novu.models.operations.ContextsControllerCreateContextResponse;
import co.novu.models.operations.ContextsControllerDeleteContextRequest;
import co.novu.models.operations.ContextsControllerDeleteContextRequestBuilder;
import co.novu.models.operations.ContextsControllerDeleteContextResponse;
import co.novu.models.operations.ContextsControllerGetContextRequest;
import co.novu.models.operations.ContextsControllerGetContextRequestBuilder;
import co.novu.models.operations.ContextsControllerGetContextResponse;
import co.novu.models.operations.ContextsControllerListContextsRequest;
import co.novu.models.operations.ContextsControllerListContextsRequestBuilder;
import co.novu.models.operations.ContextsControllerListContextsResponse;
import co.novu.models.operations.ContextsControllerUpdateContextRequest;
import co.novu.models.operations.ContextsControllerUpdateContextRequestBuilder;
import co.novu.models.operations.ContextsControllerUpdateContextResponse;
import co.novu.operations.ContextsControllerCreateContext;
import co.novu.operations.ContextsControllerDeleteContext;
import co.novu.operations.ContextsControllerGetContext;
import co.novu.operations.ContextsControllerListContexts;
import co.novu.operations.ContextsControllerUpdateContext;
import co.novu.utils.Headers;
import co.novu.utils.Options;
import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.String;


public class Contexts {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncContexts asyncSDK;

    Contexts(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.asyncSDK = new AsyncContexts(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncContexts async() {
        return asyncSDK;
    }

    /**
     * Create a context
     * 
     * <p>Create a new context with the specified type, id, and data. Returns 409 if context already exists.
     * 
     * <p>**type** and **id** are required fields, **data** is optional, if the context already exists, it
     * returns the 409 response
     * 
     * @return The call builder
     */
    public ContextsControllerCreateContextRequestBuilder create() {
        return new ContextsControllerCreateContextRequestBuilder(sdkConfiguration);
    }

    /**
     * Create a context
     * 
     * <p>Create a new context with the specified type, id, and data. Returns 409 if context already exists.
     * 
     * <p>**type** and **id** are required fields, **data** is optional, if the context already exists, it
     * returns the 409 response
     * 
     * @param body 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ContextsControllerCreateContextResponse create(@Nonnull CreateContextRequestDto body) {
        return create(null, body, null);
    }

    /**
     * Create a context
     * 
     * <p>Create a new context with the specified type, id, and data. Returns 409 if context already exists.
     * 
     * <p>**type** and **id** are required fields, **data** is optional, if the context already exists, it
     * returns the 409 response
     * 
     * @param idempotencyKey A header for idempotency purposes
     * @param body 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ContextsControllerCreateContextResponse create(
            @Nullable String idempotencyKey, @Nonnull CreateContextRequestDto body,
            @Nullable Options options) {
        ContextsControllerCreateContextRequest request = new ContextsControllerCreateContextRequest(idempotencyKey, body);
        RequestOperation<ContextsControllerCreateContextRequest, ContextsControllerCreateContextResponse> operation
              = new ContextsControllerCreateContext.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * List all contexts
     * 
     * <p>Retrieve a paginated list of all contexts, optionally filtered by type and key pattern.
     * 
     * <p>**type** and **id** are optional fields, if provided, only contexts with the matching type and id
     * will be returned.
     * **search** is an optional field, if provided, only contexts with the matching key pattern will be
     * returned.
     * Checkout all possible parameters in the query section below for more details
     * 
     * @return The call builder
     */
    public ContextsControllerListContextsRequestBuilder list() {
        return new ContextsControllerListContextsRequestBuilder(sdkConfiguration);
    }

    /**
     * List all contexts
     * 
     * <p>Retrieve a paginated list of all contexts, optionally filtered by type and key pattern.
     * 
     * <p>**type** and **id** are optional fields, if provided, only contexts with the matching type and id
     * will be returned.
     * **search** is an optional field, if provided, only contexts with the matching key pattern will be
     * returned.
     * Checkout all possible parameters in the query section below for more details
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ContextsControllerListContextsResponse list(@Nonnull ContextsControllerListContextsRequest request) {
        return list(request, null);
    }

    /**
     * List all contexts
     * 
     * <p>Retrieve a paginated list of all contexts, optionally filtered by type and key pattern.
     * 
     * <p>**type** and **id** are optional fields, if provided, only contexts with the matching type and id
     * will be returned.
     * **search** is an optional field, if provided, only contexts with the matching key pattern will be
     * returned.
     * Checkout all possible parameters in the query section below for more details
     * 
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ContextsControllerListContextsResponse list(@Nonnull ContextsControllerListContextsRequest request, @Nullable Options options) {
        RequestOperation<ContextsControllerListContextsRequest, ContextsControllerListContextsResponse> operation
              = new ContextsControllerListContexts.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Update a context
     * 
     * <p>Update the data of an existing context.
     * **type** and **id** are required fields, **data** is required. Only the data field is updated, the
     * rest of the context is not affected.
     * If the context does not exist, it returns the 404 response
     * 
     * @return The call builder
     */
    public ContextsControllerUpdateContextRequestBuilder update() {
        return new ContextsControllerUpdateContextRequestBuilder(sdkConfiguration);
    }

    /**
     * Update a context
     * 
     * <p>Update the data of an existing context.
     * **type** and **id** are required fields, **data** is required. Only the data field is updated, the
     * rest of the context is not affected.
     * If the context does not exist, it returns the 404 response
     * 
     * @param id Context ID
     * @param type Context type
     * @param body 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ContextsControllerUpdateContextResponse update(
            @Nonnull String id, @Nonnull String type,
            @Nonnull UpdateContextRequestDto body) {
        return update(id, type, null,
            body, null);
    }

    /**
     * Update a context
     * 
     * <p>Update the data of an existing context.
     * **type** and **id** are required fields, **data** is required. Only the data field is updated, the
     * rest of the context is not affected.
     * If the context does not exist, it returns the 404 response
     * 
     * @param id Context ID
     * @param type Context type
     * @param idempotencyKey A header for idempotency purposes
     * @param body 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ContextsControllerUpdateContextResponse update(
            @Nonnull String id, @Nonnull String type,
            @Nullable String idempotencyKey, @Nonnull UpdateContextRequestDto body,
            @Nullable Options options) {
        ContextsControllerUpdateContextRequest request = new ContextsControllerUpdateContextRequest(
                id, type, idempotencyKey,
                body);
        RequestOperation<ContextsControllerUpdateContextRequest, ContextsControllerUpdateContextResponse> operation
              = new ContextsControllerUpdateContext.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Retrieve a context
     * 
     * <p>Retrieve a specific context by its type and id.
     * **type** and **id** are required fields, if the context does not exist, it returns the 404 response
     * 
     * @return The call builder
     */
    public ContextsControllerGetContextRequestBuilder get() {
        return new ContextsControllerGetContextRequestBuilder(sdkConfiguration);
    }

    /**
     * Retrieve a context
     * 
     * <p>Retrieve a specific context by its type and id.
     * **type** and **id** are required fields, if the context does not exist, it returns the 404 response
     * 
     * @param id Context ID
     * @param type Context type
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ContextsControllerGetContextResponse get(@Nonnull String id, @Nonnull String type) {
        return get(id, type, null,
            null);
    }

    /**
     * Retrieve a context
     * 
     * <p>Retrieve a specific context by its type and id.
     * **type** and **id** are required fields, if the context does not exist, it returns the 404 response
     * 
     * @param id Context ID
     * @param type Context type
     * @param idempotencyKey A header for idempotency purposes
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ContextsControllerGetContextResponse get(
            @Nonnull String id, @Nonnull String type,
            @Nullable String idempotencyKey, @Nullable Options options) {
        ContextsControllerGetContextRequest request = new ContextsControllerGetContextRequest(id, type, idempotencyKey);
        RequestOperation<ContextsControllerGetContextRequest, ContextsControllerGetContextResponse> operation
              = new ContextsControllerGetContext.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Delete a context
     * 
     * <p>Delete a context by its type and id.
     * **type** and **id** are required fields, if the context does not exist, it returns the 404 response
     * 
     * @return The call builder
     */
    public ContextsControllerDeleteContextRequestBuilder delete() {
        return new ContextsControllerDeleteContextRequestBuilder(sdkConfiguration);
    }

    /**
     * Delete a context
     * 
     * <p>Delete a context by its type and id.
     * **type** and **id** are required fields, if the context does not exist, it returns the 404 response
     * 
     * @param id Context ID
     * @param type Context type
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ContextsControllerDeleteContextResponse delete(@Nonnull String id, @Nonnull String type) {
        return delete(id, type, null,
            null);
    }

    /**
     * Delete a context
     * 
     * <p>Delete a context by its type and id.
     * **type** and **id** are required fields, if the context does not exist, it returns the 404 response
     * 
     * @param id Context ID
     * @param type Context type
     * @param idempotencyKey A header for idempotency purposes
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ContextsControllerDeleteContextResponse delete(
            @Nonnull String id, @Nonnull String type,
            @Nullable String idempotencyKey, @Nullable Options options) {
        ContextsControllerDeleteContextRequest request = new ContextsControllerDeleteContextRequest(id, type, idempotencyKey);
        RequestOperation<ContextsControllerDeleteContextRequest, ContextsControllerDeleteContextResponse> operation
              = new ContextsControllerDeleteContext.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
