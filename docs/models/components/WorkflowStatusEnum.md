# WorkflowStatusEnum

Status of the workflow

## Example Usage

```java
import co.novu.models.components.WorkflowStatusEnum;

WorkflowStatusEnum value = WorkflowStatusEnum.ACTIVE;

// Open enum: use .of() to create instances from custom string values
WorkflowStatusEnum custom = WorkflowStatusEnum.of("custom_value");
```


## Values

| Name       | Value      |
| ---------- | ---------- |
| `ACTIVE`   | ACTIVE     |
| `INACTIVE` | INACTIVE   |
| `ERROR`    | ERROR      |