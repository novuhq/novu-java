# MonthlyType

## Example Usage

```java
import co.novu.models.components.MonthlyType;

MonthlyType value = MonthlyType.EACH;

// Open enum: use .of() to create instances from custom string values
MonthlyType custom = MonthlyType.of("custom_value");
```


## Values

| Name   | Value  |
| ------ | ------ |
| `EACH` | each   |
| `ON`   | on     |