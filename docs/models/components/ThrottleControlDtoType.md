# ThrottleControlDtoType

The type of throttle window.

## Example Usage

```java
import co.novu.models.components.ThrottleControlDtoType;

ThrottleControlDtoType value = ThrottleControlDtoType.FIXED;

// Open enum: use .of() to create instances from custom string values
ThrottleControlDtoType custom = ThrottleControlDtoType.of("custom_value");
```


## Values

| Name      | Value     |
| --------- | --------- |
| `FIXED`   | fixed     |
| `DYNAMIC` | dynamic   |