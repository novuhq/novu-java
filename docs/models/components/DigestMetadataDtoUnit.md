# DigestMetadataDtoUnit

Unit of the digest

## Example Usage

```java
import co.novu.models.components.DigestMetadataDtoUnit;

DigestMetadataDtoUnit value = DigestMetadataDtoUnit.SECONDS;

// Open enum: use .of() to create instances from custom string values
DigestMetadataDtoUnit custom = DigestMetadataDtoUnit.of("custom_value");
```


## Values

| Name      | Value     |
| --------- | --------- |
| `SECONDS` | seconds   |
| `MINUTES` | minutes   |
| `HOURS`   | hours     |
| `DAYS`    | days      |
| `WEEKS`   | weeks     |
| `MONTHS`  | months    |