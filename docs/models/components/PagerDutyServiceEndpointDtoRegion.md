# PagerDutyServiceEndpointDtoRegion

PagerDuty account region — determines the events API data-center endpoint.

## Example Usage

```java
import co.novu.models.components.PagerDutyServiceEndpointDtoRegion;

PagerDutyServiceEndpointDtoRegion value = PagerDutyServiceEndpointDtoRegion.US;

// Open enum: use .of() to create instances from custom string values
PagerDutyServiceEndpointDtoRegion custom = PagerDutyServiceEndpointDtoRegion.of("custom_value");
```


## Values

| Name  | Value |
| ----- | ----- |
| `US`  | us    |
| `EU`  | eu    |