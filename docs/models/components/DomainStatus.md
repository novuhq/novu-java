# DomainStatus

## Example Usage

```java
import co.novu.models.components.DomainStatus;

DomainStatus value = DomainStatus.PENDING;

// Open enum: use .of() to create instances from custom string values
DomainStatus custom = DomainStatus.of("custom_value");
```


## Values

| Name       | Value      |
| ---------- | ---------- |
| `PENDING`  | pending    |
| `VERIFIED` | verified   |