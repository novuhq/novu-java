# On

## Example Usage

```java
import co.novu.models.components.On;

On value = On.SUBSCRIBER;

// Open enum: use .of() to create instances from custom string values
On custom = On.of("custom_value");
```


## Values

| Name         | Value        |
| ------------ | ------------ |
| `SUBSCRIBER` | subscriber   |
| `PAYLOAD`    | payload      |